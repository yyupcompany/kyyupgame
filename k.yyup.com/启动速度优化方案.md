# 启动速度优化方案

## 🚀 立即可实施的优化

### 1. 优化数据库连接配置
```bash
# 修改 server/src/config/database-unified.ts
# 增加连接池大小，减少连接验证时间
pool: {
  max: 10,  # 从5增加到10
  min: 2,   # 从0增加到2，保持热连接
  acquire: 15000,  # 从30000减少到15000
  idle: 5000       # 从10000减少到5000
}
```

### 2. 启用数据库连接缓存
```bash
# 在 server/.env 中添加
DB_CONNECTION_CACHE=true
DB_CONNECTION_RETRY=3
DB_CONNECTION_TIMEOUT=5000
```

### 3. 跳过端口检查（快速启动模式）
```bash
# 创建快速启动脚本
# client/package.json 添加：
"dev:fast": "vite --host 0.0.0.0 --port 5173 --force"

# server/package.json 添加：
"dev:fast": "cross-env NODE_ENV=development PORT=3000 ts-node src/app.ts"
```

### 4. 优化TypeScript编译
```bash
# 安装ts-node缓存
npm install --save-dev ts-node-dev

# 修改server/package.json启动脚本：
"dev:fast": "ts-node-dev --respawn --transpile-only src/app.ts"
```

### 5. 减少依赖预构建
```bash
# 修改 client/vite.config.ts
optimizeDeps: {
  include: ['vue', 'vue-router', 'pinia', 'element-plus'],
  exclude: ['@element-plus/icons-vue', 'echarts']
}
```

## 📦 长期优化方案

### 1. 依赖包优化
- 移除未使用的依赖包
- 使用轻量级替代方案
- 升级到最新版本获得性能提升

### 2. 代码分割优化
- 实施更细粒度的代码分割
- 按需加载大型组件
- 优化打包配置

### 3. 数据库查询优化
- 添加适当的索引
- 优化初始化查询
- 实施查询缓存

## ⚡ 快速启动命令

### 前端快速启动
```bash
cd client
npm run dev:fast
```

### 后端快速启动
```bash
cd server
npm run dev:fast
```

### 同时快速启动
```bash
# 在根目录创建快速启动脚本
npm run dev:quick
```

## 📊 预期效果

实施这些优化后，预计启动时间可以从10秒减少到3-5秒：

- 数据库连接优化：减少2-3秒
- 跳过端口检查：减少1-2秒
- TypeScript编译优化：减少1-2秒
- 依赖预构建优化：减少1-2秒

## 🔧 监控和测试

使用以下命令测试启动时间：
```bash
# 前端启动时间测试
time npm run dev:fast

# 后端启动时间测试
time npm run dev:fast

# 完整系统启动测试
time npm run dev:quick
```