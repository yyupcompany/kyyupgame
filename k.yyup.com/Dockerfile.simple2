# 使用我们已有的Node镜像，更简单
FROM node:18-alpine

# 设置工作目录
WORKDIR /project

# 复制整个项目到容器中
COPY . /project/

# 设置环境变量
ENV PROJECT_PATH=/project

# 暴露端口
EXPOSE 3000 5173 80

# 保持容器运行
CMD ["tail", "-f", "/dev/null"]