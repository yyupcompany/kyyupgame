# 通知中心增强开发方案（Admin & 园长共用）

## 📋 现状分析

### 当前通知页面情况

| 角色 | 通知页面 | 路径 | 状态 | 说明 |
|------|---------|------|------|------|
| **教师** | ✅ 专属页面 | `/teacher-center/notifications/index.vue` | 完善 | 1095行，功能齐全 |
| **园长** | ✅ 共用页面 | `/pages/Notifications.vue` | 基础 | 808行，需增强 |
| **管理员** | ✅ 共用页面 | `/pages/Notifications.vue` | 基础 | 808行，需增强 |

### 架构设计原则

**✅ 正确的做法**:
- 教师使用专属页面 `/teacher-center/notifications/`（执行导向）
- Admin和园长共用 `/pages/Notifications.vue`（管理导向）
- 在共用页面中根据角色显示不同功能

**❌ 错误的做法**:
- 为园长单独创建 `/pages/principal/notifications/` 目录
- 代码重复，维护困难

### 问题分析

1. **共用通知页面功能不足**
   - 当前 `/pages/Notifications.vue` 功能基础
   - 缺少客户申请审批功能
   - 需要增强以支持园长的管理需求

2. **客户申请审批功能缺失**
   - 教师申请客户后，园长无法在通知中心直接审批
   - 需要跳转到其他页面处理
   - 用户体验不佳

3. **通知类型不够丰富**
   - 当前只有：SYSTEM, ACTIVITY, SCHEDULE, MESSAGE
   - 缺少：CUSTOMER_APPLICATION（客户申请）

---

## 🎯 开发目标

### 1. 增强共用通知中心页面

**路径**: `/pages/Notifications.vue`（Admin & 园长共用）

**核心功能**:
- ✅ 通知统计卡片（未读、总数、紧急、今日）
- ✅ 通知分类筛选（系统、活动、客户申请、日程等）
- ✅ 通知列表（支持批量操作）
- ✅ 客户申请审批（园长可见，直接在通知中心处理）
- ✅ 通知详情弹窗
- ✅ 实时通知提醒
- ✅ 角色权限控制（根据角色显示不同功能）

### 2. 集成客户申请审批功能

**功能**:
- 教师申请客户 → 园长收到通知
- 园长在通知中心直接审批（同意/拒绝）
- 审批后自动发送结果通知给教师
- 支持批量审批
- **角色控制**: 只有园长和管理员可以看到审批按钮

### 3. 增强通知系统

**新增通知类型**:
- `CUSTOMER_APPLICATION` - 客户申请通知

**新增通知状态**:
- `PENDING_APPROVAL` - 待审批
- `APPROVED` - 已同意
- `REJECTED` - 已拒绝

---

## 📐 页面设计

### 共用通知中心界面结构（Admin & 园长）

```
┌─────────────────────────────────────────────────────────────┐
│ 通知中心                                    [全部已读] [刷新] │
├─────────────────────────────────────────────────────────────┤
│ 📊 统计卡片                                                  │
│ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│ │ 未读通知  │ │ 总通知数  │ │ 待审批   │ │ 今日通知  │       │
│ │   15     │ │   128    │ │    8     │ │   23     │       │
│ └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
├─────────────────────────────────────────────────────────────┤
│ 🔍 筛选区域                                                  │
│ [全部] [系统通知] [客户申请] [活动通知] [日程通知]          │
│ 状态: [全部▼] 优先级: [全部▼] 关键词: [_______] [搜索]     │
├─────────────────────────────────────────────────────────────┤
│ 📋 通知列表                                                  │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 🔴 [客户申请] 教师张三申请跟踪3个客户                    │ │
│ │    申请理由：这些客户是我之前联系过的...                 │ │
│ │    2025-10-05 14:30  [查看详情] [同意] [拒绝]          │ │
│ ├─────────────────────────────────────────────────────────┤ │
│ │ 🟡 [系统通知] 系统将于今晚22:00进行维护                  │ │
│ │    预计维护时间：2小时                                   │ │
│ │    2025-10-05 10:00  [查看详情]                         │ │
│ ├─────────────────────────────────────────────────────────┤ │
│ │ 🟢 [活动通知] 亲子活动报名已满                           │ │
│ │    当前报名人数：50/50                                   │ │
│ │    2025-10-04 16:20  [查看详情]                         │ │
│ └─────────────────────────────────────────────────────────┘ │
│ [1] [2] [3] ... [10]                                        │
└─────────────────────────────────────────────────────────────┘
```

### 客户申请审批弹窗

```
┌─────────────────────────────────────────────────────────────┐
│ 客户申请审批                                      [×]        │
├─────────────────────────────────────────────────────────────┤
│ 申请信息                                                     │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 申请教师：张三                                           │ │
│ │ 申请时间：2025-10-05 14:30                              │ │
│ │ 申请客户数：3个                                          │ │
│ │ 申请理由：这些客户是我之前在活动中接触过的，            │ │
│ │           他们对我们的课程很感兴趣...                    │ │
│ └─────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│ 客户列表                                                     │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ □ 客户姓名  电话        来源    状态    当前分配         │ │
│ │ ☑ 李四     138****1234  网络    新客户  未分配          │ │
│ │ ☑ 王五     139****5678  电话    已联系  未分配          │ │
│ │ ☑ 赵六     137****9012  推荐    意向    刘老师          │ │
│ └─────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│ 审批操作                                                     │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 审批意见：                                               │ │
│ │ [_____________________________________________________] │ │
│ │                                                          │ │
│ │ [全部同意] [全部拒绝] [部分同意]                        │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                          [取消] [确认审批]  │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔧 技术实现

### 1. 页面组件结构

**主页面**: `/pages/Notifications.vue`（已存在，需增强）

**新增组件**:
```
pages/Notifications.vue                # 主页面（增强）
components/notifications/              # 新增组件目录
├── CustomerApplicationCard.vue        # 客户申请卡片
├── ApplicationReviewDialog.vue        # 审批弹窗
└── NotificationActionButtons.vue      # 操作按钮（根据角色显示）
```

**复用现有组件**:
- 统计卡片、筛选组件、通知列表等已有组件继续使用

### 2. 核心功能实现

#### 2.1 角色权限控制

```typescript
import { useUserStore } from '@/stores/user';

const userStore = useUserStore();
const userRole = computed(() => userStore.role);

// 判断是否可以审批客户申请
const canApproveApplication = computed(() => {
  return userRole.value === 'principal' || userRole.value === 'admin';
});

// 在模板中使用
<template>
  <div v-if="canApproveApplication" class="approval-actions">
    <el-button @click="handleApprove">同意</el-button>
    <el-button @click="handleReject">拒绝</el-button>
  </div>
</template>
```

#### 2.2 通知统计

```typescript
interface NotificationStats {
  unread: number;           // 未读通知数
  total: number;            // 总通知数
  pendingApproval: number;  // 待审批数（仅园长和管理员可见）
  today: number;            // 今日通知数
  urgent: number;           // 紧急通知数
}

const loadNotificationStats = async () => {
  const response = await getNotificationStats();
  notificationStats.value = response.data;
};
```

#### 2.3 客户申请通知处理

```typescript
interface CustomerApplicationNotification {
  id: number;
  type: 'CUSTOMER_APPLICATION';
  title: string;
  content: string;
  applicationId: number;      // 关联的申请ID
  teacherId: number;          // 申请教师ID
  teacherName: string;        // 申请教师姓名
  customerCount: number;      // 申请客户数
  applyReason: string;        // 申请理由
  status: 'pending' | 'approved' | 'rejected';
  createdAt: string;
}

const handleApproveApplication = async (notification: CustomerApplicationNotification) => {
  // 检查权限
  if (!canApproveApplication.value) {
    ElMessage.warning('您没有审批权限');
    return;
  }

  // 打开审批弹窗
  currentApplication.value = notification;
  reviewDialogVisible.value = true;
};

const submitReview = async (action: 'approve' | 'reject', reason?: string) => {
  await reviewCustomerApplication(
    currentApplication.value.applicationId,
    action,
    reason
  );

  // 刷新通知列表
  await loadNotifications();

  ElMessage.success(action === 'approve' ? '已同意申请' : '已拒绝申请');
};
```

#### 2.4 实时通知

```typescript
// 使用WebSocket或轮询实现实时通知
const setupRealtimeNotification = () => {
  // 每30秒检查一次新通知
  setInterval(async () => {
    const response = await getNotificationStats();
    if (response.data.unread > notificationStats.value.unread) {
      // 有新通知
      ElNotification({
        title: '新通知',
        message: `您有 ${response.data.unread - notificationStats.value.unread} 条新通知`,
        type: 'info',
        duration: 3000
      });

      // 刷新通知列表
      await loadNotifications();
    }
  }, 30000);
};
```

---

## 📊 数据流程

### 完整的客户申请审批流程

```
教师端                     后端API                    园长端
  │                          │                          │
  ├─ 1. 提交申请              │                          │
  │   POST /api/teacher/     │                          │
  │   customer-applications  │                          │
  │                          │                          │
  │                          ├─ 2. 创建申请记录          │
  │                          │   (customer_applications)│
  │                          │                          │
  │                          ├─ 3. 创建通知             │
  │                          │   POST /api/notifications│
  │                          │   type: CUSTOMER_APPLICATION
  │                          │   userId: principalId    │
  │                          │                          │
  │                          │                          ├─ 4. 通知中心显示
  │                          │                          │   新申请（红点提醒）
  │                          │                          │
  │                          │                          ├─ 5. 点击查看详情
  │                          │                          │   打开审批弹窗
  │                          │                          │
  │                          │                          ├─ 6. 审批操作
  │                          │                          │   [同意] / [拒绝]
  │                          │                          │
  │                          │<─ 7. 提交审批 ───────────┤
  │                          │   POST /api/principal/   │
  │                          │   customer-applications/ │
  │                          │   :id/review             │
  │                          │                          │
  │                          ├─ 8. 更新申请状态          │
  │                          │   + 分配客户（如果同意）  │
  │                          │                          │
  │                          ├─ 9. 创建结果通知          │
  │                          │   POST /api/notifications│
  │                          │   userId: teacherId      │
  │                          │                          │
  │<─ 10. 教师收到通知 ────────┤                          │
  │    "您的申请已通过"        │                          │
```

---

## 🎨 UI/UX设计要点

### 1. 通知优先级视觉区分

| 优先级 | 颜色 | 图标 | 说明 |
|--------|------|------|------|
| 紧急 | 🔴 红色 | ⚠️ | 需要立即处理 |
| 重要 | 🟡 黄色 | ⭐ | 需要尽快处理 |
| 普通 | 🟢 绿色 | ℹ️ | 正常处理 |

### 2. 通知类型图标

| 类型 | 图标 | 说明 |
|------|------|------|
| 客户申请 | 👥 | 教师申请客户 |
| 系统通知 | 🔔 | 系统消息 |
| 活动通知 | 🎉 | 活动相关 |
| 日程通知 | 📅 | 日程提醒 |

### 3. 交互设计

- **快速审批**: 在通知列表直接显示[同意][拒绝]按钮
- **批量操作**: 支持批量标记已读、批量审批
- **实时更新**: 新通知自动显示，无需刷新
- **消息提醒**: 新通知弹出桌面通知

---

## 📝 开发任务清单

### 阶段1: 增强现有通知页面（1.5天）

- [ ] 在 `/pages/Notifications.vue` 中添加角色权限控制
- [ ] 增强统计卡片（添加待审批统计）
- [ ] 增强筛选功能（添加客户申请类型）
- [ ] 优化通知列表显示

### 阶段2: 客户申请审批功能（2天）

- [ ] 创建 `components/notifications/CustomerApplicationCard.vue`
- [ ] 创建 `components/notifications/ApplicationReviewDialog.vue`
- [ ] 创建 `components/notifications/NotificationActionButtons.vue`
- [ ] 集成客户申请API
- [ ] 实现审批逻辑
- [ ] 实现通知发送

### 阶段3: 增强功能（1天）

- [ ] 实现实时通知
- [ ] 实现批量操作
- [ ] 优化用户体验
- [ ] 添加加载动画

### 阶段4: 测试和优化（1天）

- [ ] 单元测试
- [ ] 集成测试
- [ ] 角色权限测试
- [ ] 性能优化
- [ ] 文档编写

**总工作量**: 5.5天

---

## ✅ 总结

### 开发内容

1. ✅ **增强共用通知中心页面**
   - 路径: `/pages/Notifications.vue`（Admin & 园长共用）
   - 添加角色权限控制
   - 功能完善，界面专业

2. ✅ **集成客户申请审批功能**
   - 直接在通知中心审批
   - 支持批量审批
   - 自动发送结果通知
   - 只有园长和管理员可以审批

3. ✅ **增强通知系统**
   - 新增客户申请通知类型
   - 实时通知提醒
   - 优化用户体验

### 架构优势

- ✅ **代码复用**: Admin和园长共用同一页面，减少重复代码
- ✅ **统一维护**: 只需维护一个通知中心页面
- ✅ **角色控制**: 根据角色显示不同功能，灵活可控
- ✅ **易于扩展**: 未来添加新角色或功能更容易

### 预期效果

- Admin和园长可以在共用通知中心查看所有通知
- 客户申请通知会突出显示，方便快速审批
- 审批后自动通知教师，流程完整流转
- 界面美观，操作便捷
- 教师继续使用专属的通知中心页面

---

**需要我开始开发吗？** 🚀

我会按照这个方案，增强共用通知中心页面，确保客户申请审批流程完美流转！

