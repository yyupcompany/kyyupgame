# 表格列配置标准化指南

## 📋 概述

为了统一所有表格的列配置,避免重复代码和不一致的样式,我们创建了全局的表格列配置工具。

**工具文件**: `client/src/utils/table-config.ts`

---

## 🎯 核心优势

### 1. 统一标准
- ✅ 所有表格遵循统一的列宽度、对齐方式、tooltip规则
- ✅ 避免每个页面重复配置相同的属性
- ✅ 确保用户体验一致性

### 2. 简化开发
- ✅ 一行代码创建标准列配置
- ✅ 减少90%的重复代码
- ✅ 降低出错概率

### 3. 易于维护
- ✅ 修改一处,全局生效
- ✅ 集中管理所有列配置规则
- ✅ 便于后续优化和调整

---

## 📐 标准列宽度

| 列类型 | 宽度 | 对齐方式 | Tooltip | 说明 |
|--------|------|----------|---------|------|
| ID | 80px | 右对齐 | 否 | 数字ID |
| 序号 | 60px | 居中 | 否 | 自动序号 |
| 选择框 | 55px | 居中 | 否 | 多选框 |
| 姓名(短) | 120px | 左对齐 | 是 | 短姓名 |
| 姓名(中) | 150px | 左对齐 | 是 | 中等姓名 |
| 姓名(长) | 180px | 左对齐 | 是 | 长姓名/超长ID |
| 电话 | 130px | 居中 | 是 | 手机号码 |
| 邮箱 | 200px | 左对齐 | 是 | 邮箱地址 |
| 状态 | 100px | 居中 | 否 | 状态标签 |
| 日期 | 120px | 居中 | 否 | 日期(YYYY-MM-DD) |
| 日期时间 | 160px | 居中 | 是 | 日期时间 |
| 数字 | 100-150px | 右对齐 | 否 | 数字 |
| 金额 | 120px | 右对齐 | 否 | 金额 |
| 百分比 | 100px | 右对齐 | 否 | 百分比 |
| 文本(短) | 120px | 左对齐 | 是 | 短文本 |
| 文本(中) | 180px | 左对齐 | 是 | 中等文本 |
| 文本(长) | 250px | 左对齐 | 是 | 长文本 |
| 操作(小) | 120px | 居中 | 否 | 1-2个按钮 |
| 操作(中) | 180px | 居中 | 否 | 2-3个按钮 |
| 操作(大) | 200px | 居中 | 否 | 3-4个按钮 |

---

## 🚀 快速开始

### 基础用法

```typescript
import { 
  createIdColumn, 
  createNameColumn, 
  createPhoneColumn, 
  createStatusColumn,
  createDateColumn,
  createActionsColumn 
} from '@/utils/table-config'

// 定义表格列
const columns = [
  createIdColumn(),                           // ID列
  createNameColumn('客户姓名'),               // 姓名列
  createPhoneColumn(),                        // 电话列
  createStatusColumn('状态'),                 // 状态列
  createDateColumn('创建时间', 'createdAt'),  // 日期列
  createActionsColumn()                       // 操作列
]
```

### 自定义配置

```typescript
// 可以覆盖默认配置
const columns = [
  createIdColumn('用户ID', { sortable: false }),
  createNameColumn('客户姓名', 'name', 'long', { fixed: 'left' }),
  createPhoneColumn('手机号', 'mobile', { width: 150 }),
  createActionsColumn('操作', 'large', { fixed: 'right' })
]
```

---

## 📚 API文档

### 1. createIdColumn()

创建ID列配置

```typescript
createIdColumn(
  label?: string = 'ID',
  options?: Partial<TableColumnConfig>
): TableColumnConfig
```

**示例**:
```typescript
createIdColumn()                    // 默认配置
createIdColumn('用户ID')            // 自定义标签
createIdColumn('ID', { width: 100 }) // 自定义宽度
```

---

### 2. createNameColumn()

创建姓名列配置

```typescript
createNameColumn(
  label?: string = '姓名',
  prop?: string = 'name',
  size?: 'short' | 'medium' | 'long' = 'long',
  options?: Partial<TableColumnConfig>
): TableColumnConfig
```

**参数**:
- `label`: 列标题
- `prop`: 数据字段名
- `size`: 列宽度尺寸
  - `short`: 120px - 适用于短姓名
  - `medium`: 150px - 适用于中等姓名
  - `long`: 180px - 适用于长姓名或超长ID
- `options`: 自定义配置

**示例**:
```typescript
createNameColumn('客户姓名')                        // 默认long尺寸
createNameColumn('学生姓名', 'studentName', 'short') // short尺寸
createNameColumn('姓名', 'name', 'long', { fixed: 'left' }) // 固定左侧
```

---

### 3. createPhoneColumn()

创建电话列配置

```typescript
createPhoneColumn(
  label?: string = '联系电话',
  prop?: string = 'phone',
  options?: Partial<TableColumnConfig>
): TableColumnConfig
```

**示例**:
```typescript
createPhoneColumn()                     // 默认配置
createPhoneColumn('手机号', 'mobile')   // 自定义字段
```

---

### 4. createEmailColumn()

创建邮箱列配置

```typescript
createEmailColumn(
  label?: string = '邮箱',
  prop?: string = 'email',
  options?: Partial<TableColumnConfig>
): TableColumnConfig
```

---

### 5. createStatusColumn()

创建状态列配置

```typescript
createStatusColumn(
  label?: string = '状态',
  prop?: string = 'status',
  options?: Partial<TableColumnConfig>
): TableColumnConfig
```

---

### 6. createDateColumn()

创建日期列配置

```typescript
createDateColumn(
  label: string,
  prop: string,
  options?: Partial<TableColumnConfig>
): TableColumnConfig
```

**示例**:
```typescript
createDateColumn('创建时间', 'createdAt')
createDateColumn('入学时间', 'enrollDate', { sortable: true })
```

---

### 7. createDateTimeColumn()

创建日期时间列配置

```typescript
createDateTimeColumn(
  label: string,
  prop: string,
  options?: Partial<TableColumnConfig>
): TableColumnConfig
```

---

### 8. createNumberColumn()

创建数字列配置

```typescript
createNumberColumn(
  label: string,
  prop: string,
  size?: 'short' | 'medium' | 'long' = 'medium',
  options?: Partial<TableColumnConfig>
): TableColumnConfig
```

**示例**:
```typescript
createNumberColumn('年龄', 'age', 'short')
createNumberColumn('数量', 'count', 'medium')
```

---

### 9. createMoneyColumn()

创建金额列配置

```typescript
createMoneyColumn(
  label: string,
  prop: string,
  options?: Partial<TableColumnConfig>
): TableColumnConfig
```

---

### 10. createTextColumn()

创建文本列配置

```typescript
createTextColumn(
  label: string,
  prop: string,
  size?: 'short' | 'medium' | 'long' = 'medium',
  options?: Partial<TableColumnConfig>
): TableColumnConfig
```

---

### 11. createActionsColumn()

创建操作列配置

```typescript
createActionsColumn(
  label?: string = '操作',
  size?: 'small' | 'medium' | 'large' = 'medium',
  options?: Partial<TableColumnConfig>
): TableColumnConfig
```

**参数**:
- `size`: 操作列宽度
  - `small`: 120px - 1-2个按钮
  - `medium`: 180px - 2-3个按钮
  - `large`: 200px - 3-4个按钮

**示例**:
```typescript
createActionsColumn()                           // 默认medium尺寸
createActionsColumn('操作', 'small')            // small尺寸
createActionsColumn('操作', 'large', { fixed: 'right' }) // 固定右侧
```

---

## 🎨 完整示例

### 示例1: 学生管理表格

```typescript
import { 
  createIdColumn, 
  createNameColumn, 
  createTextColumn,
  createNumberColumn,
  createStatusColumn,
  createDateColumn,
  createActionsColumn 
} from '@/utils/table-config'

const studentsColumns = [
  createIdColumn(),
  createNameColumn('学生姓名', 'name', 'long', { fixed: 'left' }),
  createTextColumn('学号', 'studentId', 'long'),
  createTextColumn('班级', 'className', 'medium'),
  createTextColumn('性别', 'gender', 'short', { align: 'center' }),
  createNumberColumn('年龄', 'age', 'short'),
  createStatusColumn('状态'),
  createDateColumn('入学时间', 'enrollDate'),
  createActionsColumn('操作', 'medium', { fixed: 'right' })
]
```

### 示例2: 客户管理表格

```typescript
const customersColumns = [
  createIdColumn('客户ID', { align: 'right' }),
  createNameColumn('客户姓名', 'name', 'long'),
  createPhoneColumn('联系电话', 'phone'),
  createEmailColumn('邮箱', 'email'),
  createStatusColumn('状态', 'status'),
  createTextColumn('来源', 'source', 'medium'),
  createDateTimeColumn('创建时间', 'createdAt'),
  createActionsColumn('操作', 'large')
]
```

### 示例3: 教师管理表格

```typescript
const teachersColumns = [
  createIdColumn(),
  createNameColumn('教师姓名', 'name', 'medium'),
  createPhoneColumn('手机号', 'mobile'),
  createEmailColumn('邮箱'),
  createTextColumn('任教班级', 'classes', 'medium'),
  createStatusColumn('在职状态', 'employmentStatus'),
  createDateColumn('入职时间', 'hireDate'),
  createActionsColumn()
]
```

---

## 🔄 迁移指南

### 迁移前

```typescript
const columns = [
  { prop: 'id', label: 'ID', width: 80, align: 'right' as const },
  { prop: 'name', label: '客户姓名', width: 180, showOverflowTooltip: true, align: 'left' as const },
  { prop: 'phone', label: '联系电话', width: 130, showOverflowTooltip: true, align: 'center' as const },
  { prop: 'status', label: '状态', width: 100, align: 'center' as const },
  { prop: 'createdAt', label: '创建时间', width: 160, align: 'center' as const },
  { prop: 'actions', label: '操作', type: 'actions', width: 180, fixed: 'right' as const, align: 'center' as const }
]
```

### 迁移后

```typescript
import { createIdColumn, createNameColumn, createPhoneColumn, createStatusColumn, createDateTimeColumn, createActionsColumn } from '@/utils/table-config'

const columns = [
  createIdColumn(),
  createNameColumn('客户姓名'),
  createPhoneColumn(),
  createStatusColumn('状态'),
  createDateTimeColumn('创建时间', 'createdAt'),
  createActionsColumn()
]
```

**代码减少**: 从6行 × 平均80字符 = 480字符 → 6行 × 平均40字符 = 240字符  
**减少比例**: 50%

---

## ✅ 最佳实践

### 1. 优先使用标准函数

```typescript
// ✅ 推荐
createNameColumn('客户姓名')

// ❌ 不推荐
{ prop: 'name', label: '客户姓名', width: 180, showOverflowTooltip: true, align: 'left' }
```

### 2. 根据内容选择合适的尺寸

```typescript
// 短姓名 (如"张三")
createNameColumn('姓名', 'name', 'short')

// 长姓名或ID (如"parent_1752837104412_0_0")
createNameColumn('客户姓名', 'name', 'long')
```

### 3. 固定重要列

```typescript
// 固定左侧
createNameColumn('姓名', 'name', 'long', { fixed: 'left' })

// 固定右侧
createActionsColumn('操作', 'medium', { fixed: 'right' })
```

### 4. 启用排序

```typescript
createDateColumn('创建时间', 'createdAt', { sortable: true })
createNumberColumn('年龄', 'age', 'short', { sortable: true })
```

---

## 🎯 下一步

1. ✅ 已创建全局配置工具: `client/src/utils/table-config.ts`
2. ⏳ 迁移现有页面使用新配置
3. ⏳ 更新所有中心页面
4. ⏳ 生成迁移报告

---

**文档版本**: 1.0.0  
**创建时间**: 2025年10月13日  
**维护者**: 开发团队

