# 全局样式修复计划 - 详细版

**创建日期**: 2025-10-25  
**目标**: 系统地修复所有页面的样式，确保使用统一的设计令牌

---

## 📋 修复优先级

### 第1优先级 - 核心页面 (Week 1)
这些页面是系统的核心，使用频率最高，应该首先修复。

1. **Dashboard** (`client/src/pages/dashboard/index.vue`)
   - 状态: 部分使用设计令牌
   - 需要修复: 硬编码的颜色、间距
   - 预计时间: 2小时

2. **Login** (`client/src/pages/Login/index.vue`)
   - 状态: 需要检查
   - 需要修复: 可能有硬编码的样式
   - 预计时间: 1小时

3. **所有中心页面** (`client/src/pages/centers/`)
   - 状态: 大部分已使用统一样式
   - 需要修复: 细节优化
   - 预计时间: 3小时

### 第2优先级 - 业务页面 (Week 2)
这些页面是主要的业务功能页面。

1. **活动管理** (`client/src/pages/activity/`)
2. **招生管理** (`client/src/pages/enrollment/`)
3. **班级管理** (`client/src/pages/class/`)
4. **学生管理** (`client/src/pages/student/`)
5. **教师管理** (`client/src/pages/teacher/`)
6. **家长管理** (`client/src/pages/parent/`)

### 第3优先级 - 系统页面 (Week 3)
这些页面是系统管理和配置页面。

1. **系统管理** (`client/src/pages/system/`)
2. **权限管理** (`client/src/pages/permissions/`)
3. **用户管理** (`client/src/pages/users/`)

### 第4优先级 - 其他页面 (Week 4)
其他功能页面和特殊页面。

1. **AI功能** (`client/src/pages/ai/`)
2. **营销管理** (`client/src/pages/marketing/`)
3. **统计分析** (`client/src/pages/statistics/`)
4. **错误页面** (`404.vue`, `403.vue`)

---

## 🔧 修复步骤

### 步骤1: 检查页面结构
```vue
<!-- ❌ 不推荐 -->
<div class="page-wrapper">
  <div class="content">
    <!-- 内容 -->
  </div>
</div>

<!-- ✅ 推荐 -->
<div class="center-container">
  <!-- 内容 -->
</div>
```

### 步骤2: 替换硬编码颜色
```scss
/* ❌ 不推荐 */
color: #f8fafc;
background: #1e293b;
border-color: #334155;

/* ✅ 推荐 */
color: var(--text-primary);
background: var(--bg-secondary);
border-color: var(--border-color);
```

### 步骤3: 替换硬编码间距
```scss
/* ❌ 不推荐 */
padding: 24px;
margin: 16px;
gap: 12px;

/* ✅ 推荐 */
padding: var(--spacing-xl);
margin: var(--spacing-lg);
gap: var(--spacing-md);
```

### 步骤4: 替换硬编码字体
```scss
/* ❌ 不推荐 */
font-size: 16px;
font-weight: 600;
line-height: 1.5;

/* ✅ 推荐 */
font-size: var(--text-base);
font-weight: var(--font-semibold);
line-height: var(--line-height-normal);
```

### 步骤5: 替换硬编码阴影
```scss
/* ❌ 不推荐 */
box-shadow: 0 4px 6px rgba(0, 0, 0, 0.4);

/* ✅ 推荐 */
box-shadow: var(--shadow-md);
```

---

## 📊 修复检查清单

对于每个页面，检查以下项目：

### 容器和布局
- [ ] 使用了统一的容器类 (`center-container` 或 `page-container`)
- [ ] 移除了自定义的容器样式
- [ ] 响应式布局正常

### 颜色
- [ ] 所有文字颜色使用 `--text-*` 变量
- [ ] 所有背景颜色使用 `--bg-*` 变量
- [ ] 所有边框颜色使用 `--border-*` 变量
- [ ] 所有功能色使用对应的 `--*-color` 变量

### 间距
- [ ] 所有 padding 使用 `--spacing-*` 变量
- [ ] 所有 margin 使用 `--spacing-*` 变量
- [ ] 所有 gap 使用 `--spacing-*` 变量

### 字体
- [ ] 所有 font-size 使用 `--text-*` 变量
- [ ] 所有 font-weight 使用 `--font-*` 变量
- [ ] 所有 line-height 使用 `--line-height-*` 变量

### 阴影和效果
- [ ] 所有 box-shadow 使用 `--shadow-*` 变量
- [ ] 所有 border-radius 使用 `--radius-*` 变量
- [ ] 所有过渡使用 `--transition-*` 变量

### 主题适配
- [ ] 暗黑主题显示正常
- [ ] 浅色主题显示正常
- [ ] 主题切换流畅

---

## 🎯 修复示例

### 示例1: 简单页面修复

**修复前**:
```vue
<template>
  <div class="my-page">
    <div class="header">
      <h1>页面标题</h1>
    </div>
    <div class="content">
      <!-- 内容 -->
    </div>
  </div>
</template>

<style scoped>
.my-page {
  background: #1e293b;
  padding: 24px;
}

.header {
  margin-bottom: 24px;
  padding-bottom: 16px;
  border-bottom: 1px solid #334155;
}

.header h1 {
  color: #f8fafc;
  font-size: 24px;
  font-weight: 700;
  margin: 0;
}

.content {
  color: #cbd5e1;
}
</style>
```

**修复后**:
```vue
<template>
  <div class="center-container">
    <div class="page-header">
      <h1>页面标题</h1>
    </div>
    <div class="page-content">
      <!-- 内容 -->
    </div>
  </div>
</template>

<style scoped>
.page-header {
  margin-bottom: var(--spacing-xl);
  padding-bottom: var(--spacing-lg);
  border-bottom: 1px solid var(--border-color);
}

.page-header h1 {
  color: var(--text-primary);
  font-size: var(--text-2xl);
  font-weight: var(--font-bold);
  margin: 0;
}

.page-content {
  color: var(--text-secondary);
}
</style>
```

---

## 📈 预期效果

### 完成后的指标
- ✅ 100% 的页面使用设计令牌
- ✅ 0 个硬编码的颜色值
- ✅ 0 个硬编码的间距值
- ✅ 0 个硬编码的字体大小
- ✅ 0 个硬编码的阴影值
- ✅ 完美的主题切换体验
- ✅ 完美的响应式设计

---

**状态**: 待实施  
**优先级**: 高  
**预计完成时间**: 4周

