# 代码库结构导航

## 🎯 核心入口文件
- `client/src/main.ts` - Vue 3 前端应用入口
- `server/src/index.ts` - Express 后端服务器入口
- `client/src/App.vue` - 根组件
- `client/src/router/index.ts` - 路由配置

## 📁 关键目录结构

### 前端 (client/)
```
client/src/
├── components/          # 可复用组件库
│   ├── common/         # 通用组件 (按钮、表单等)
│   ├── layout/         # 布局组件 (侧边栏、导航等)
│   └── [feature]/      # 功能特定组件
├── pages/              # 页面组件
│   ├── dashboard/      # 仪表板页面
│   ├── ai/            # AI助手页面
│   ├── system/        # 系统管理页面
│   └── [module]/      # 其他业务模块
├── api/               # API 客户端
│   ├── endpoints.ts   # API 端点定义
│   └── modules/       # 按模块分组的 API 调用
├── stores/            # Pinia 状态管理
├── utils/             # 工具函数
└── types/             # TypeScript 类型定义
```

### 后端 (server/)
```
server/src/
├── controllers/        # 控制器 (处理 HTTP 请求)
├── models/            # 数据模型 (Sequelize ORM)
├── routes/            # 路由定义
├── services/          # 业务逻辑服务
│   ├── ai/           # AI 相关服务
│   └── system/       # 系统服务
├── middlewares/       # 中间件
├── utils/             # 工具函数
└── config/            # 配置文件
```

## 🔄 数据流向

### 前端请求流程
```
Vue Component → API Client → HTTP Request → Backend Route → Controller → Service → Model → Database
```

### 用户认证流程
```
Login Form → Auth API → JWT Token → Store in Pinia → Route Guards → Permission Check
```

## 🎨 UI 组件体系

### 主要组件
- `MainLayout.vue` - 主布局容器
- `Sidebar.vue` - 侧边栏导航
- `Navbar.vue` - 顶部导航栏
- `AIAssistant.vue` - AI 助手组件

### 通用组件
- `PageHeader.vue` - 页面标题
- `LoadingSpinner.vue` - 加载动画
- `EmptyState.vue` - 空状态展示

## 🔐 权限系统

### 角色定义
- **Admin**: 系统管理员 (全部权限)
- **Principal**: 园长 (管理权限)
- **Teacher**: 教师 (教学权限)
- **Parent**: 家长 (查看权限)

### 权限检查位置
- `client/src/router/index.ts` - 路由守卫
- `server/src/middlewares/auth.middleware.ts` - API 权限中间件
- `client/src/utils/permission.ts` - 前端权限工具

## 🤖 AI 功能架构

### AI 服务组件
- `server/src/services/ai/ai-memory.service.ts` - AI 记忆管理
- `server/src/services/ai/message.service.ts` - 消息处理
- `server/src/services/ai/model.service.ts` - 模型管理
- `client/src/pages/ai/AIAssistantPage.vue` - AI 助手界面

## 📊 核心业务模块

### 招生管理
- **路径**: `client/src/pages/enrollment-plan/`
- **API**: `server/src/routes/enrollment-plan.routes.ts`
- **模型**: `server/src/models/enrollment-plan.model.ts`

### 学生管理
- **路径**: `client/src/pages/student/`
- **API**: `server/src/routes/student.routes.ts`
- **模型**: `server/src/models/student.model.ts`

### 教师管理
- **路径**: `client/src/pages/teacher/`
- **API**: `server/src/routes/teacher.routes.ts`
- **模型**: `server/src/models/teacher.model.ts`

## 🛠️ 开发工具配置

### 构建配置
- `client/vite.config.ts` - Vite 构建配置
- `client/tsconfig.json` - TypeScript 配置
- `server/tsconfig.json` - 后端 TS 配置

### 测试配置
- `client/vitest.config.ts` - 前端测试配置
- `client/playwright.config.ts` - E2E 测试配置

## 🔍 常见问题位置

### API 集成问题
- 检查: `client/src/api/interceptors.ts`
- 检查: `client/src/utils/request.ts`

### 路由问题
- 检查: `client/src/router/index.ts`
- 检查: 页面组件的路由定义

### 权限问题
- 检查: `server/src/middlewares/auth.middleware.ts`
- 检查: `client/src/utils/permission.ts`

### 数据库问题
- 检查: `server/src/models/index.ts`
- 检查: 具体模型文件

## 💡 快速定位指南

### 要修改某个页面
1. 在 `client/src/pages/` 找到对应页面
2. 检查相关组件在 `client/src/components/`
3. 查看 API 调用在 `client/src/api/modules/`

### 要修改 API
1. 在 `server/src/routes/` 找到路由
2. 在 `server/src/controllers/` 找到控制器
3. 在 `server/src/services/` 查看业务逻辑

### 要添加新功能
1. 设计数据模型 (`server/src/models/`)
2. 创建 API 路由 (`server/src/routes/`)
3. 实现前端页面 (`client/src/pages/`)
4. 添加 API 集成 (`client/src/api/modules/`)