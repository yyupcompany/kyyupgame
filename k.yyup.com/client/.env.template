# ======================================
# 环境配置模板 - 环境变量管理系统
# ======================================
# 使用说明：
# 1. 复制此文件到具体环境配置文件
# 2. 根据环境替换 ${PLACEHOLDER} 占位符
# 3. 确保所有域名和端口通过环境变量管理
# ======================================

# 环境标识
VITE_NODE_ENV=${NODE_ENV}
VITE_APP_ENV=${APP_ENV}

# API 配置系统
# 开发环境API代理目标 - 支持IPv4和IPv6
VITE_API_PROXY_TARGET=${API_PROXY_TARGET}

# API基础URL - 开发环境留空使用代理，生产环境使用完整URL
VITE_API_BASE_URL=${API_BASE_URL}

# API域名列表，逗号分隔 - 用于跨域和CORS配置
VITE_API_DOMAINS=${API_DOMAINS}

# 应用配置系统
VITE_APP_URL=${APP_URL}
VITE_APP_NAME=${APP_NAME}
VITE_APP_VERSION=${APP_VERSION}

# 统一租户中心配置
VITE_UNIFIED_TENANT_URL=${UNIFIED_TENANT_URL}
VITE_TENANT_DOMAIN=${TENANT_DOMAIN}

# 开发服务器配置
VITE_DEV_HOST=${DEV_HOST}
VITE_DEV_PORT=${DEV_PORT}
VITE_HMR_HOST=${HMR_HOST}
VITE_HMR_CLIENT_PORT=${HMR_CLIENT_PORT}
VITE_HMR_PROTOCOL=${HMR_PROTOCOL}
VITE_WS_URL=${WS_URL}

# 路由配置系统
VITE_ROUTER_WHITE_LIST=${ROUTER_WHITE_LIST}
VITE_ADMIN_ROLES=${ADMIN_ROLES}
VITE_LOGIN_PATH=${LOGIN_PATH}
VITE_HOME_PATH=${HOME_PATH}

# 文件上传配置
VITE_UPLOAD_URL=${UPLOAD_URL}
VITE_UPLOAD_SIZE_LIMIT=${UPLOAD_SIZE_LIMIT}

# 认证配置
VITE_TOKEN_EXPIRE_TIME=${TOKEN_EXPIRE_TIME}

# API超时配置
VITE_API_TIMEOUT=${API_TIMEOUT}

# 功能开关
VITE_ENABLE_MOCK=${ENABLE_MOCK}
VITE_ENABLE_DEBUG=${ENABLE_DEBUG}

# 性能配置
VITE_BUNDLE_ANALYZER=${BUNDLE_ANALYZER}
VITE_SOURCEMAP=${SOURCEMAP}

# 构建配置
VITE_BUILD_OUTDIR=${BUILD_OUTDIR}
VITE_PUBLIC_PATH=${PUBLIC_PATH}