# 前端开发服务器脚本说明

## 🎯 异常监测版本启动脚本

### 功能特性

✅ **自动异常监测**: 监控进程错误和异常，防止服务意外退出
✅ **智能重启机制**: 遇到严重错误时自动重启，最多重试10次
✅ **端口冲突处理**: 自动检测并清理端口占用
✅ **分级错误处理**: 区分警告、一般错误和严重错误
✅ **优雅关闭**: 支持Ctrl+C优雅关闭服务
✅ **状态监控**: 定期显示服务状态信息

### 使用方法

#### 1. 安全启动模式 (推荐)
```bash
cd /home/devbox/project/client
npm run dev:safe
```

#### 2. 传统启动模式
```bash
cd /home/devbox/project/client
npm run dev
```

### 脚本对比

| 特性 | 传统模式 (dev) | 安全模式 (dev:safe) |
|------|---------------|-------------------|
| 基础服务启动 | ✅ | ✅ |
| 端口清理 | ✅ | ✅ |
| 异常监测 | ❌ | ✅ |
| 自动重启 | ❌ | ✅ |
| 错误分级 | ❌ | ✅ |
| 优雅关闭 | ❌ | ✅ |
| 状态监控 | ❌ | ✅ |

### 异常处理机制

#### 错误分类

1. **警告级别** (继续运行):
   - 弃用警告 (deprecated)
   - HMR热更新信息
   - SourceMap警告
   - 依赖警告

2. **一般错误** (记录但继续运行):
   - 编译错误
   - 类型检查错误
   - 轻微的配置问题

3. **严重错误** (触发重启):
   - 端口已被占用 (EADDRINUSE)
   - 模块解析失败 (Cannot resolve)
   - 模块找不到 (Module not found)
   - 语法错误 (Syntax error)
   - 构建失败 (Build failed)
   - 文件不存在 (ENOENT)

#### 重启策略

- **重启次数限制**: 最多10次自动重启
- **渐进式延迟**: 第1次重启等待5秒，第2次10秒，依此类推，最多30秒
- **计数重置**: 服务成功启动后重启计数清零

### 输出示例

#### 正常启动
```
🎯 前端开发服务器 (带异常监测)
══════════════════════════════════════════════════
✅ 自动重启: 启用
✅ 异常监测: 启用
✅ 端口清理: 启用
══════════════════════════════════════════════════
🚀 启动前端开发服务器...
📍 工作目录: /home/devbox/project/client
⏰ 启动时间: 7/15/2025, 10:47:43 AM
🔍 清理端口占用...
✅ 端口清理完成
🔧 启动Vite开发服务器...
📊 Vite进程ID: 12345
✅ 前端服务启动成功!
```

#### 异常重启
```
❌ 服务异常退出: code=1, signal=null
🔄 将在 5 秒后重启服务 (第1次重启)
重启原因: 异常退出 (code=1, signal=null)
🔄 正在重启服务...
```

#### 状态监控 (每5分钟显示)
```
📊 服务状态:
├─ 重启次数: 0/10
├─ 进程ID: 12345
├─ 运行状态: 运行中
└─ 启动时间: 7/15/2025, 10:47:43 AM
```

### 关闭服务

#### 优雅关闭
```bash
# 按 Ctrl+C
🛑 接收到信号 SIGINT，正在优雅关闭...
🧹 正在清理Vite进程...
✅ 服务已关闭
```

#### 强制关闭
```bash
# 如果优雅关闭失败，5秒后强制终止
🔥 强制终止Vite进程...
```

### 日志文件

脚本运行时的日志信息会显示在控制台，如需保存日志可以使用重定向：

```bash
# 保存日志到文件
npm run dev:safe > dev.log 2>&1

# 实时查看日志
npm run dev:safe | tee dev.log
```

### 故障排除

#### 问题1: 脚本无法启动
```bash
# 检查Node.js版本
node --version

# 检查脚本权限
ls -la scripts/dev-with-error-handling.cjs

# 手动添加执行权限
chmod +x scripts/dev-with-error-handling.cjs
```

#### 问题2: 端口清理失败
```bash
# 手动检查端口占用
netstat -tulpn | grep 5173

# 手动清理端口
bash scripts/kill-ports.sh
```

#### 问题3: 重启次数超限
```bash
# 检查系统资源
free -h
df -h

# 检查错误日志
tail -n 50 dev.log
```

### 文件说明

- `dev-with-error-handling.cjs`: 主要的异常监测启动脚本
- `dev-with-error-handling.js`: ES模块版本 (已废弃)
- `kill-ports.sh`: 端口清理脚本
- `README.md`: 本说明文档

### 建议用法

1. **开发阶段**: 使用 `npm run dev:safe` 获得稳定的开发环境
2. **调试阶段**: 使用 `npm run dev` 获得更直接的错误信息
3. **生产环境**: 使用 `npm run build:prod` 构建生产版本

### 更新记录

- **v1.0.0**: 初始版本，支持基本的异常监测和自动重启
- **v1.0.1**: 添加错误分级处理，优化重启策略
- **v1.0.2**: 改用CommonJS模块，提高兼容性