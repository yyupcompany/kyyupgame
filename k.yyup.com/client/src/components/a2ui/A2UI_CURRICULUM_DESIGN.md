# A2UI 幼儿园课件设计规范

## 1. 课件基础规范

### 1.1 尺寸标准
```
课件尺寸: 1920 × 1080 像素 (16:9 比例)
安全区域: 1760 × 960 像素 (边距 80px)
触屏热区: 最小 80 × 80 像素
```

### 1.2 页面类型
| 类型 | 用途 | 布局特点 |
|------|------|----------|
| `title` | 标题页 | 课程名称、学习目标、封面图 |
| `content` | 内容页 | 图文混排、知识点展示 |
| `activity` | 互动页 | 选择题、拖拽排序、拼图等 |
| `media` | 媒体页 | 图片轮播、视频播放 |
| `summary` | 总结页 | 知识回顾、得分展示 |

### 1.3 分页导航
- 不允许页面滚动
- 必须有"上一页"/"下一页"按钮
- 必须显示页码指示器 (如: 3/10)
- 支持触屏滑动切换

---

## 2. 字体规范

### 2.1 字体层级
```scss
// 超大标题 - 课程名称
$font-title-hero: 72px;     // 行高 1.2
$font-weight-hero: 800;

// 大标题 - 页面标题
$font-title-large: 56px;    // 行高 1.3
$font-weight-large: 700;

// 中标题 - 活动标题
$font-title-medium: 44px;   // 行高 1.4
$font-weight-medium: 600;

// 正文 - 内容文字
$font-body: 36px;           // 行高 1.6
$font-weight-body: 400;

// 提示文字 - 操作提示
$font-hint: 28px;           // 行高 1.5
$font-weight-hint: 400;

// 按钮文字
$font-button-large: 32px;
$font-button-medium: 28px;
$font-button-small: 24px;
```

### 2.2 字体颜色
```scss
$color-text-primary: #333333;      // 主要文字
$color-text-secondary: #666666;    // 次要文字
$color-text-hint: #999999;         // 提示文字
$color-text-white: #FFFFFF;        // 白色文字（深色背景）
$color-text-success: #52c41a;      // 正确反馈
$color-text-error: #ff4d4f;        // 错误反馈
$color-text-warning: #faad14;      // 警告文字
```

---

## 3. 按钮规范

### 3.1 按钮尺寸
```scss
// 超大按钮 - 主要操作（提交答案、开始游戏）
$button-xl: {
  width: 320px,
  height: 96px,
  border-radius: 48px,
  font-size: 36px
};

// 大按钮 - 选项按钮
$button-lg: {
  width: 240px,
  height: 72px,
  border-radius: 36px,
  font-size: 32px
};

// 中按钮 - 导航按钮（上一页/下一页）
$button-md: {
  width: 160px,
  height: 56px,
  border-radius: 28px,
  font-size: 28px
};

// 小按钮 - 辅助操作
$button-sm: {
  width: 120px,
  height: 48px,
  border-radius: 24px,
  font-size: 24px
};
```

### 3.2 按钮颜色
```scss
// 主要按钮 - 渐变色
$button-primary: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
$button-primary-hover: linear-gradient(135deg, #5a6fd6 0%, #6a4190 100%);
$button-primary-active: linear-gradient(135deg, #4e5fc2 0%, #5e377e 100%);

// 成功按钮 - 正确答案
$button-success: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);

// 警告按钮 - 需要注意
$button-warning: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);

// 次要按钮 - 浅色背景
$button-secondary: {
  background: #f5f7fa,
  border: 2px solid #e4e7ed,
  color: #606266
};
```

### 3.3 按钮动画
```scss
// 点击缩放
@keyframes button-tap {
  0% { transform: scale(1); }
  50% { transform: scale(0.95); }
  100% { transform: scale(1); }
}

// 成功弹跳
@keyframes button-success {
  0%, 100% { transform: scale(1); }
  25% { transform: scale(1.1); }
  50% { transform: scale(0.95); }
  75% { transform: scale(1.05); }
}

// 错误抖动
@keyframes button-error {
  0%, 100% { transform: translateX(0); }
  20%, 60% { transform: translateX(-10px); }
  40%, 80% { transform: translateX(10px); }
}
```

---

## 4. 选项卡片规范

### 4.1 选项尺寸
```scss
// 两列选项
$option-2col: {
  width: 800px,
  height: 120px,
  gap: 40px
};

// 三列选项（图片选项）
$option-3col: {
  width: 520px,
  height: 400px,
  gap: 40px
};

// 四列选项（小图片）
$option-4col: {
  width: 380px,
  height: 300px,
  gap: 30px
};
```

### 4.2 选项样式
```scss
.option-card {
  background: #ffffff;
  border: 4px solid #e4e7ed;
  border-radius: 24px;
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
  transition: all 0.3s ease;
  cursor: pointer;
  
  // 悬停状态
  &:hover {
    border-color: #667eea;
    transform: translateY(-4px);
    box-shadow: 0 12px 32px rgba(102, 126, 234, 0.2);
  }
  
  // 选中状态
  &.selected {
    border-color: #667eea;
    background: linear-gradient(135deg, #f5f7ff 0%, #ede9fe 100%);
    box-shadow: 0 8px 24px rgba(102, 126, 234, 0.3);
  }
  
  // 正确状态
  &.correct {
    border-color: #52c41a;
    background: linear-gradient(135deg, #f6ffed 0%, #d9f7be 100%);
    animation: option-correct 0.5s ease;
  }
  
  // 错误状态
  &.wrong {
    border-color: #ff4d4f;
    background: linear-gradient(135deg, #fff2f0 0%, #ffccc7 100%);
    animation: option-wrong 0.5s ease;
  }
}
```

### 4.3 选项动画
```scss
// 正确选项动画
@keyframes option-correct {
  0% { transform: scale(1); }
  25% { transform: scale(1.05); }
  50% { transform: scale(0.98); }
  100% { transform: scale(1); }
}

// 错误选项抖动
@keyframes option-wrong {
  0%, 100% { transform: translateX(0); }
  20%, 60% { transform: translateX(-8px); }
  40%, 80% { transform: translateX(8px); }
}

// 选项入场动画
@keyframes option-enter {
  0% { opacity: 0; transform: translateY(30px); }
  100% { opacity: 1; transform: translateY(0); }
}
```

---

## 5. 图片规范

### 5.1 图片尺寸
```scss
// 全屏背景
$image-fullscreen: 1920 × 1080;

// 半屏图片（左右分栏）
$image-half: 800 × 600;

// 内容图片
$image-content: 600 × 450;

// 选项图片
$image-option: 400 × 300;

// 图标图片
$image-icon: 120 × 120;
```

### 5.2 图片样式
```scss
.curriculum-image {
  border-radius: 16px;
  object-fit: cover;
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
  
  // 图片加载动画
  animation: image-fade-in 0.5s ease;
}

@keyframes image-fade-in {
  0% { opacity: 0; transform: scale(0.95); }
  100% { opacity: 1; transform: scale(1); }
}
```

---

## 6. 页面布局模板

### 6.1 标题页布局
```
┌─────────────────────────────────────────────────────────────┐
│                        [背景图/渐变]                          │
│                                                              │
│                    ╔═══════════════════╗                     │
│                    ║   🎓 课程标题      ║   ← 72px 居中        │
│                    ╚═══════════════════╝                     │
│                                                              │
│                      副标题/年龄段                             │  ← 36px
│                                                              │
│                   ┌─────────────────┐                        │
│                   │   [开始上课]     │                        │  ← 主按钮
│                   └─────────────────┘                        │
│                                                              │
│  [页码: 1/10]                              [上一页] [下一页]   │
└─────────────────────────────────────────────────────────────┘
```

### 6.2 内容页布局（左图右文）
```
┌─────────────────────────────────────────────────────────────┐
│  ┌───────────────────────┐  ┌────────────────────────────┐  │
│  │                       │  │  📚 知识点标题              │  │  ← 56px
│  │                       │  ├────────────────────────────┤  │
│  │      [内容图片]        │  │                            │  │
│  │      800×600          │  │  知识点内容文字说明          │  │  ← 36px
│  │                       │  │  可以是多行文字              │  │
│  │                       │  │                            │  │
│  │                       │  │  💡 小提示：补充说明         │  │  ← 28px
│  └───────────────────────┘  └────────────────────────────┘  │
│                                                              │
│  [1/10]                                    [上一页] [下一页]  │
└─────────────────────────────────────────────────────────────┘
```

### 6.3 选择题页布局
```
┌─────────────────────────────────────────────────────────────┐
│                                                              │
│                  ❓ 问题标题文字（44px）                       │
│                     提示文字（28px）                          │
│                                                              │
│     ┌────────────────┐        ┌────────────────┐            │
│     │  [图片/文字]    │        │  [图片/文字]    │            │
│     │   选项 A        │        │   选项 B        │            │
│     └────────────────┘        └────────────────┘            │
│                                                              │
│     ┌────────────────┐        ┌────────────────┐            │
│     │  [图片/文字]    │        │  [图片/文字]    │            │
│     │   选项 C        │        │   选项 D        │            │
│     └────────────────┘        └────────────────┘            │
│                                                              │
│                    ┌──────────────┐                          │
│                    │  [提交答案]   │                          │
│                    └──────────────┘                          │
│  [得分: 0/100]  [3/10]                     [上一页] [下一页]  │
└─────────────────────────────────────────────────────────────┘
```

### 6.4 拖拽排序页布局
```
┌─────────────────────────────────────────────────────────────┐
│                                                              │
│               🔢 请把下面的步骤按正确顺序排列                  │
│                                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐     │
│  │  步骤 1   │  │  步骤 2   │  │  步骤 3   │  │  步骤 4   │     │
│  │  [图片]   │  │  [图片]   │  │  [图片]   │  │  [图片]   │     │
│  │  文字     │  │  文字     │  │  文字     │  │  文字     │     │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘     │
│       ↕              ↕              ↕              ↕         │
│   [拖拽区域 - 可交换位置]                                     │
│                                                              │
│                    ┌──────────────┐                          │
│                    │  [检查答案]   │                          │
│                    └──────────────┘                          │
│  [得分: 0/100]  [5/10]                     [上一页] [下一页]  │
└─────────────────────────────────────────────────────────────┘
```

### 6.5 总结页布局
```
┌─────────────────────────────────────────────────────────────┐
│                                                              │
│                       🎉 课程完成！                          │
│                                                              │
│                   ┌─────────────────┐                        │
│                   │    ⭐⭐⭐⭐⭐     │                        │
│                   │   得分: 85/100   │                        │
│                   │   用时: 5分30秒   │                        │
│                   └─────────────────┘                        │
│                                                              │
│                   📝 今天学到了：                             │
│                   ✓ 知识点 1                                  │
│                   ✓ 知识点 2                                  │
│                   ✓ 知识点 3                                  │
│                                                              │
│     ┌──────────────┐              ┌──────────────┐           │
│     │  [再来一次]   │              │  [结束课程]   │           │
│     └──────────────┘              └──────────────┘           │
│  [10/10]                                   [上一页]          │
└─────────────────────────────────────────────────────────────┘
```

---

## 7. 动画规范

### 7.1 页面切换动画
```scss
// 向左滑出（下一页）
@keyframes slide-out-left {
  0% { transform: translateX(0); opacity: 1; }
  100% { transform: translateX(-100%); opacity: 0; }
}

// 向右滑入（下一页）
@keyframes slide-in-right {
  0% { transform: translateX(100%); opacity: 0; }
  100% { transform: translateX(0); opacity: 1; }
}

// 向右滑出（上一页）
@keyframes slide-out-right {
  0% { transform: translateX(0); opacity: 1; }
  100% { transform: translateX(100%); opacity: 0; }
}

// 向左滑入（上一页）
@keyframes slide-in-left {
  0% { transform: translateX(-100%); opacity: 0; }
  100% { transform: translateX(0); opacity: 1; }
}
```

### 7.2 元素入场动画
```scss
// 标题入场
@keyframes title-enter {
  0% { opacity: 0; transform: translateY(-30px) scale(0.9); }
  100% { opacity: 1; transform: translateY(0) scale(1); }
}

// 内容入场（延迟）
@keyframes content-enter {
  0% { opacity: 0; transform: translateY(20px); }
  100% { opacity: 1; transform: translateY(0); }
}

// 按钮入场（弹性）
@keyframes button-enter {
  0% { opacity: 0; transform: scale(0.5); }
  60% { transform: scale(1.1); }
  100% { opacity: 1; transform: scale(1); }
}
```

### 7.3 反馈动画
```scss
// 正确答案 - 星星飞出
@keyframes stars-burst {
  0% { opacity: 1; transform: scale(0) rotate(0deg); }
  50% { opacity: 1; transform: scale(1.5) rotate(180deg); }
  100% { opacity: 0; transform: scale(2) rotate(360deg); }
}

// 错误答案 - 红叉抖动
@keyframes wrong-shake {
  0%, 100% { transform: translateX(0) rotate(0deg); }
  25% { transform: translateX(-10px) rotate(-5deg); }
  75% { transform: translateX(10px) rotate(5deg); }
}

// 得分增加
@keyframes score-pop {
  0% { transform: scale(1); }
  50% { transform: scale(1.3); color: #52c41a; }
  100% { transform: scale(1); }
}
```

### 7.4 触屏交互动画
```scss
// 触摸反馈
@keyframes touch-ripple {
  0% {
    transform: scale(0);
    opacity: 0.5;
  }
  100% {
    transform: scale(4);
    opacity: 0;
  }
}

// 拖拽时的悬浮
.dragging {
  transform: scale(1.05);
  box-shadow: 0 16px 48px rgba(0, 0, 0, 0.2);
  z-index: 1000;
}
```

---

## 8. 课件数据结构

### 8.1 课件主结构
```typescript
interface Curriculum {
  id: string;
  name: string;
  description: string;
  
  // 保存原始提示词（用于编辑和重新生成）
  originalPrompt: string;
  
  // AI对话历史（用于追溯修改）
  editHistory: EditHistoryItem[];
  
  // 课件配置
  config: {
    ageGroup: 'small' | 'middle' | 'large';  // 小班/中班/大班
    domain: string;                           // 领域
    duration: number;                         // 预计时长（分钟）
    totalScore: number;                       // 总分
  };
  
  // 主题样式
  theme: CurriculumTheme;
  
  // 幻灯片页面
  slides: Slide[];
  
  // 媒体资源
  media: MediaResource[];
  
  // 元数据
  metadata: {
    createdAt: Date;
    updatedAt: Date;
    version: number;
    author: string;
  };
}

interface EditHistoryItem {
  id: string;
  timestamp: Date;
  type: 'ai_edit' | 'manual_edit';
  prompt?: string;           // AI编辑时的提示词
  changes: string;           // 变更描述
  beforeSnapshot: string;    // 修改前快照
}

interface CurriculumTheme {
  primaryColor: string;
  secondaryColor: string;
  backgroundColor: string;
  backgroundImage?: string;
  fontFamily: string;
}
```

### 8.2 幻灯片结构
```typescript
interface Slide {
  id: string;
  type: 'title' | 'content' | 'activity' | 'media' | 'summary';
  
  // 布局配置
  layout: {
    template: string;        // 布局模板名称
    columns?: number;        // 列数
    alignment?: string;      // 对齐方式
  };
  
  // 页面组件
  components: SlideComponent[];
  
  // 入场动画配置
  enterAnimation: {
    type: string;
    duration: number;
    delay: number;
  };
  
  // 背景配置
  background: {
    type: 'color' | 'gradient' | 'image';
    value: string;
  };
  
  // 音频配置
  audio?: {
    ttsText?: string;        // TTS文字
    audioUrl?: string;       // 音频文件
    autoPlay: boolean;
    playDelay: number;
  };
}

interface SlideComponent {
  id: string;
  type: string;              // 组件类型
  position: {
    x: number;               // 相对位置 (0-1920)
    y: number;               // 相对位置 (0-1080)
    width: number;
    height: number;
    zIndex: number;
  };
  props: Record<string, any>;
  animation?: {
    enter: string;
    enterDelay: number;
    interaction?: string;
  };
}
```

---

## 9. AI生成提示词模板

### 9.1 系统提示词
```
你是一位专业的幼儿园课件设计师。请根据用户需求生成符合以下规范的课件JSON数据：

【课件规格】
- 尺寸：1920×1080 像素，16:9 比例
- 不允许滚动，必须分页展示
- 每页必须包含导航按钮（上一页/下一页）和页码

【字体规范】
- 课程标题：72px，粗体
- 页面标题：56px，半粗体
- 活动标题：44px，半粗体
- 正文内容：36px，常规
- 提示文字：28px，常规

【按钮规范】
- 主要按钮：320×96px，渐变色背景
- 选项按钮：240×72px，带边框
- 导航按钮：160×56px，次要样式

【课件结构】
1. 标题页（1页）：课程名称、学习目标、开始按钮
2. 内容页（2-4页）：知识点讲解，图文混排
3. 互动页（3-5页）：选择题、拖拽排序等互动活动
4. 总结页（1页）：知识回顾、得分展示

【互动设计原则】
- 选项数量：2-4个，幼儿易于选择
- 图片要大而清晰，吸引注意力
- 反馈要即时，有动画和声音
- 鼓励为主，避免负面评价

【输出格式】
输出完整的课件JSON，包含所有页面和组件配置。
```

### 9.2 课件生成提示词示例
```
请为【大班(5-6岁)】设计一节【科学领域】的课程。

课程主题：交通安全 - 认识交通标志
学习目标：
1. 认识3种常见交通标志
2. 掌握过马路的正确步骤
3. 辨别危险行为

请生成包含以下内容的课件：
- 1个标题页
- 2个知识讲解页（图文并茂）
- 3个互动活动页（选择题和拖拽排序）
- 1个总结页

图片风格：卡通可爱，色彩鲜艳
```

---

## 10. 编辑模式设计

### 10.1 编辑功能
- **AI对话编辑**：通过对话修改课件内容
- **可视化编辑**：拖拽组件、调整位置
- **属性面板**：修改文字、颜色、动画等
- **页面管理**：添加/删除/重排页面

### 10.2 编辑界面布局
```
┌─────────────────────────────────────────────────────────────────────────┐
│  [保存] [预览] [导出]                     课件名称                [设置] │
├─────────────┬───────────────────────────────────────────┬───────────────┤
│             │                                           │               │
│  📄 页面列表 │           [当前页面预览区域]              │  📝 属性面板   │
│             │            1920 × 1080                    │               │
│  [+添加页]   │                                           │  组件属性     │
│             │                                           │  - 文字       │
│  ▸ 标题页   │                                           │  - 颜色       │
│  ▸ 内容页1  │                                           │  - 位置       │
│  ▸ 互动页1  │                                           │  - 动画       │
│  ▸ 互动页2  │                                           │               │
│  ▸ 总结页   │                                           │  页面属性     │
│             │                                           │  - 背景       │
│             │                                           │  - 音频       │
│             │                                           │               │
├─────────────┴───────────────────────────────────────────┴───────────────┤
│  💬 AI助手对话框                                                         │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │ 用户: 把第3页的问题改成"红灯亮了应该怎么办？"                          │ │
│  │ AI: 好的，我已经修改了第3页的问题标题，选项也相应更新了。               │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│  [输入修改需求...]                                              [发送]   │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 11. 组件清单

### 11.1 布局组件
| 组件 | 用途 |
|------|------|
| `SlideContainer` | 幻灯片容器，1920×1080 |
| `SlideNavigation` | 页面导航（上/下页，页码） |
| `SlideGrid` | 网格布局 |
| `SlideSplit` | 左右/上下分栏 |

### 11.2 内容组件
| 组件 | 用途 |
|------|------|
| `SlideTitle` | 标题文字 |
| `SlideText` | 正文段落 |
| `SlideImage` | 图片展示 |
| `SlideVideo` | 视频播放 |

### 11.3 互动组件
| 组件 | 用途 |
|------|------|
| `SlideChoice` | 选择题（单选/多选） |
| `SlideDragSort` | 拖拽排序 |
| `SlidePuzzle` | 拼图游戏 |
| `SlideMatching` | 连线配对 |

### 11.4 反馈组件
| 组件 | 用途 |
|------|------|
| `SlideScoreBoard` | 得分展示 |
| `SlideFeedback` | 答题反馈 |
| `SlideProgress` | 进度指示 |
| `SlideCelebration` | 完成庆祝 |

---

## 12. 最佳实践

### 12.1 设计原则
1. **大字体**：确保后排学生也能看清
2. **高对比度**：颜色对比明显
3. **简洁布局**：每页信息量适中
4. **即时反馈**：操作后立即有响应
5. **积极鼓励**：多用正面语言

### 12.2 互动设计
1. **选项明确**：图文并茂，避免歧义
2. **触屏友好**：热区足够大（>80px）
3. **防误触**：重要操作需要确认
4. **可撤销**：允许修改答案

### 12.3 无障碍支持
1. **高对比模式**：支持切换
2. **字体放大**：支持调整
3. **语音播报**：TTS支持
4. **键盘导航**：支持方向键
