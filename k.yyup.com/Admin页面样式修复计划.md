# Admin 页面样式修复计划

**创建日期**: 2025-10-25  
**优先级**: 高  
**预计工作量**: 2-3小时

---

## 📋 修复清单

### 1. Backup.vue - 硬编码值替换

**文件**: `client/src/pages/system/Backup.vue`

**问题**:
```scss
// ❌ 第70行
.page-header {
  margin-bottom: 20px;  // 硬编码值
}

// ❌ 第86行
.development-placeholder h2 {
  margin: var(--spacing-lg) 0 10px 0;  // 混合使用
}
```

**修复方案**:
```scss
// ✅ 修复后
.page-header {
  margin-bottom: var(--spacing-xl);  // 20px = var(--spacing-xl)
}

.development-placeholder h2 {
  margin: var(--spacing-lg) 0 var(--spacing-sm) 0;  // 10px = var(--spacing-sm)
}
```

**修复步骤**:
1. 打开 `client/src/pages/system/Backup.vue`
2. 找到第70行的 `.page-header` 样式
3. 将 `margin-bottom: 20px;` 替换为 `margin-bottom: var(--spacing-xl);`
4. 找到第86行的 `.development-placeholder h2` 样式
5. 将 `margin: var(--spacing-lg) 0 10px 0;` 替换为 `margin: var(--spacing-lg) 0 var(--spacing-sm) 0;`
6. 保存文件

---

### 2. 全局字体大小检查

**问题**: 多个页面使用了硬编码的字体大小

**检查范围**:
- `client/src/pages/system/Log.vue`
- `client/src/pages/system/Role.vue`
- `client/src/pages/system/Permission.vue`
- `client/src/pages/system/users/index.vue`

**修复方案**:
```scss
// ❌ 硬编码值
font-size: 12px;
font-size: 14px;
font-size: 16px;

// ✅ 使用令牌
font-size: var(--text-xs);    // 12px
font-size: var(--text-sm);    // 14px
font-size: var(--text-base);  // 16px
```

**修复步骤**:
1. 搜索所有 `font-size: 12px;` 的出现
2. 替换为 `font-size: var(--text-xs);`
3. 搜索所有 `font-size: 14px;` 的出现
4. 替换为 `font-size: var(--text-sm);`
5. 搜索所有 `font-size: 16px;` 的出现
6. 替换为 `font-size: var(--text-base);`

---

### 3. 间距值标准化

**问题**: 部分页面使用了非标准的间距值

**标准间距映射**:
```
4px   → var(--spacing-xs)
8px   → var(--spacing-sm)
12px  → var(--spacing-md)
16px  → var(--spacing-lg)
24px  → var(--spacing-xl)
32px  → var(--spacing-2xl)
```

**修复步骤**:
1. 检查所有 `margin` 和 `padding` 值
2. 确保都使用标准间距
3. 替换非标准值为最接近的标准值

---

### 4. 颜色值标准化

**问题**: 确保所有颜色都使用 CSS 变量

**检查项**:
- [ ] 所有文字颜色使用 `--text-*` 变量
- [ ] 所有背景颜色使用 `--bg-*` 变量
- [ ] 所有边框颜色使用 `--border-*` 变量
- [ ] 所有功能色使用对应的 `--*-color` 变量

**修复步骤**:
1. 搜索所有硬编码的颜色值 (如 `#ffffff`, `#000000` 等)
2. 替换为对应的 CSS 变量
3. 验证暗黑模式下的显示效果

---

## 🔍 验证清单

修复完成后，需要验证以下项目:

### 视觉验证
- [ ] 所有页面在浅色主题下显示正常
- [ ] 所有页面在暗黑主题下显示正常
- [ ] 所有页面在移动端显示正常
- [ ] 所有页面在平板端显示正常
- [ ] 所有页面在桌面端显示正常

### 代码验证
- [ ] 没有硬编码的颜色值
- [ ] 没有硬编码的间距值
- [ ] 没有硬编码的字体大小
- [ ] 所有样式都使用了设计令牌

### 性能验证
- [ ] 页面加载时间 < 2秒
- [ ] 没有样式冲突
- [ ] 没有控制台错误

---

## 📊 修复进度

| 页面 | 状态 | 修复内容 | 验证状态 |
|------|------|--------|--------|
| Backup.vue | ⏳ 待修复 | 硬编码值替换 | ⏳ 待验证 |
| Log.vue | ⏳ 检查中 | 字体大小检查 | ⏳ 待验证 |
| Role.vue | ⏳ 检查中 | 字体大小检查 | ⏳ 待验证 |
| Permission.vue | ⏳ 检查中 | 字体大小检查 | ⏳ 待验证 |
| User.vue | ✅ 完成 | 已使用令牌 | ✅ 已验证 |

---

## 🎯 预期效果

修复完成后:
- ✅ 所有 Admin 页面都使用设计令牌
- ✅ 没有硬编码的样式值
- ✅ 样式完全一致
- ✅ 响应式设计完整
- ✅ 暗黑模式完美支持

---

**最后更新**: 2025-10-25

