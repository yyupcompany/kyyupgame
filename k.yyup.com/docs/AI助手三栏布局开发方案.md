# AI助手三栏布局开发方案

**创建时间**: 2025-10-06  
**状态**: 📋 待开发  
**优先级**: ⭐⭐⭐⭐⭐

---

## 📋 目录

- [需求概述](#需求概述)
- [现状分析](#现状分析)
- [设计方案](#设计方案)
- [技术实现](#技术实现)
- [开发步骤](#开发步骤)
- [注意事项](#注意事项)

---

## 🎯 需求概述

### 用户需求
在AI助手全屏模式下，实现类似 **claude.ai** 的三栏布局：

1. **左侧栏（可折叠）**
   - 对话历史列表
   - Token用量统计
   - 会话管理功能
   - 默认状态：缩小（仅显示图标）

2. **中间栏（主要区域）**
   - 聊天对话内容
   - AI回复和用户消息
   - 输入框和快捷操作

3. **右侧栏（条件显示）**
   - 工具调用状态显示
   - 组件渲染区域（图表、表格、报表等）
   - 仅在开启智能工具时显示

### 业务价值
- ✅ 提升专业感和用户体验
- ✅ 更好的空间利用率
- ✅ 清晰的功能分区
- ✅ 支持复杂的AI交互场景

---

## 📊 现状分析

### 当前架构

**文件位置**: `client/src/components/ai-assistant/AIAssistant.vue`

**当前布局结构**:
```
┌─────────────────────────────────────────────────────┐
│  头部区域 (ai-header)                                │
│  - 标题 + 操作按钮                                   │
├─────────────────────────────────────────────────────┤
│  上下文横幅 (context-banner)                         │
│  - 页面信息 + 用户角色                               │
├─────────────────────────────────────────────────────┤
│  快捷操作 (quick-actions)                            │
│  - 快捷按钮                                          │
├─────────────────────────────────────────────────────┤
│  聊天区域 (chat-area)                                │
│  - 消息列表 (max-width: 960px, 居中)                │
├─────────────────────────────────────────────────────┤
│  输入区域 (input-area)                               │
│  - 文本输入 + 功能按钮                               │
└─────────────────────────────────────────────────────┘

[左侧浮层抽屉 - el-drawer]
- 会话历史列表
- 450px 宽度
- 从左侧滑出
```

### 现有功能

**已实现**:
- ✅ 全屏模式切换 (`isFullscreen`)
- ✅ 会话历史抽屉 (`ConversationDrawer`)
- ✅ 工具调用状态显示 (`ToolCallingStatus`)
- ✅ 动态组件渲染 (`DynamicComponentRenderer`)
- ✅ 拖拽调整宽度 (非全屏模式)

**存在问题**:
- ❌ 全屏模式下是单栏布局
- ❌ 左侧抽屉是浮层，不是固定侧边栏
- ❌ 没有右侧工具渲染区域
- ❌ 没有Token用量统计显示
- ❌ 空间利用率不高

---

## 🎨 设计方案

### 三栏布局设计

```
全屏模式 (100vw x 100vh)
┌──────┬─────────────────────────────────────────┬──────────┐
│      │  头部区域 (ai-header)                    │          │
│      │  - 标题 + 操作按钮                       │          │
│      ├─────────────────────────────────────────┤          │
│      │  上下文横幅 (context-banner)             │          │
│ 左   ├─────────────────────────────────────────┤   右     │
│ 侧   │  聊天区域 (chat-area)                    │   侧     │
│ 栏   │  - 消息列表                              │   栏     │
│      │  - AI回复                                │          │
│ 可   │  - 用户消息                              │   工     │
│ 折   ├─────────────────────────────────────────┤   具     │
│ 叠   │  输入区域 (input-area)                   │   渲     │
│      │  - 文本输入 + 功能按钮                   │   染     │
└──────┴─────────────────────────────────────────┴──────────┘
 60px    自适应宽度 (flex: 1)                      400px
 (折叠)                                           (条件显示)
 280px
 (展开)
```

### 响应式宽度分配

| 状态 | 左侧栏 | 中间栏 | 右侧栏 | 说明 |
|------|--------|--------|--------|------|
| 默认 | 60px | flex: 1 | 0px | 左侧折叠，右侧隐藏 |
| 左侧展开 | 280px | flex: 1 | 0px | 显示对话历史 |
| 工具激活 | 60px | flex: 1 | 400px | 显示工具渲染 |
| 全部展开 | 280px | flex: 1 | 400px | 完整三栏模式 |

---

## 🛠️ 技术实现

### 1. 状态管理

**新增响应式状态**:
```typescript
// 左侧栏状态
const leftSidebarCollapsed = ref(true)  // 默认折叠
const leftSidebarWidth = computed(() => 
  leftSidebarCollapsed.value ? 60 : 280
)

// 右侧栏状态
const rightSidebarVisible = ref(false)  // 条件显示
const rightSidebarWidth = 400  // 固定宽度

// Token统计
const tokenUsage = ref({
  current: 0,
  total: 0,
  percentage: 0
})
```

### 2. 布局结构

**新的HTML结构**:
```vue
<template>
  <div class="ai-assistant" :class="{ 'fullscreen': isFullscreen }">
    <!-- 全屏模式：三栏布局 -->
    <div v-if="isFullscreen" class="fullscreen-layout">
      <!-- 左侧栏 -->
      <aside 
        class="left-sidebar" 
        :class="{ 'collapsed': leftSidebarCollapsed }"
        :style="{ width: leftSidebarWidth + 'px' }"
      >
        <LeftSidebar
          :collapsed="leftSidebarCollapsed"
          :conversations="conversations"
          :token-usage="tokenUsage"
          @toggle="leftSidebarCollapsed = !leftSidebarCollapsed"
          @select-conversation="switchConversation"
        />
      </aside>

      <!-- 中间主区域 -->
      <main class="center-main">
        <!-- 头部 -->
        <div class="ai-header">...</div>
        
        <!-- 上下文横幅 -->
        <div class="context-banner">...</div>
        
        <!-- 聊天区域 -->
        <div class="chat-area">...</div>
        
        <!-- 输入区域 -->
        <div class="input-area">...</div>
      </main>

      <!-- 右侧栏（条件显示） -->
      <aside 
        v-if="rightSidebarVisible" 
        class="right-sidebar"
        :style="{ width: rightSidebarWidth + 'px' }"
      >
        <RightSidebar
          :tool-calls="currentToolCalls"
          :component-data="currentComponentData"
          @close="rightSidebarVisible = false"
        />
      </aside>
    </div>

    <!-- 非全屏模式：保持原有布局 -->
    <div v-else class="normal-layout">
      <!-- 原有布局代码 -->
    </div>
  </div>
</template>
```

### 3. 样式实现

**核心CSS**:
```scss
.ai-assistant.fullscreen {
  .fullscreen-layout {
    display: flex;
    width: 100vw;
    height: 100vh;
    background: var(--el-bg-color-page);
  }

  // 左侧栏
  .left-sidebar {
    flex-shrink: 0;
    height: 100vh;
    background: var(--el-bg-color);
    border-right: 1px solid var(--el-border-color);
    transition: width 0.3s ease;
    overflow: hidden;

    &.collapsed {
      width: 60px;
    }
  }

  // 中间主区域
  .center-main {
    flex: 1;
    display: flex;
    flex-direction: column;
    height: 100vh;
    overflow: hidden;
    min-width: 600px; // 最小宽度保证可用性

    .chat-area {
      flex: 1;
      overflow-y: auto;
      padding: 20px;
    }
  }

  // 右侧栏
  .right-sidebar {
    flex-shrink: 0;
    height: 100vh;
    background: var(--el-bg-color);
    border-left: 1px solid var(--el-border-color);
    overflow-y: auto;
    animation: slideInFromRight 0.3s ease;
  }
}

@keyframes slideInFromRight {
  from {
    transform: translateX(100%);
    opacity: 0;
  }
  to {
    transform: translateX(0);
    opacity: 1;
  }
}
```

---

## 📝 开发步骤

### Phase 1: 创建新组件 (2-3小时)

**1.1 创建左侧栏组件**
```bash
client/src/components/ai-assistant/LeftSidebar.vue
```

**功能**:
- 折叠/展开切换按钮
- 对话历史列表（复用 ConversationDrawer 逻辑）
- Token用量统计卡片
- 新建会话按钮

**1.2 创建右侧栏组件**
```bash
client/src/components/ai-assistant/RightSidebar.vue
```

**功能**:
- 工具调用状态显示（复用 ToolCallingStatus）
- 动态组件渲染区域（复用 DynamicComponentRenderer）
- 关闭按钮
- 标签页切换（工具调用 / 渲染结果）

**1.3 创建Token统计组件**
```bash
client/src/components/ai-assistant/TokenUsageCard.vue
```

**功能**:
- 显示当前会话Token用量
- 显示总Token用量
- 进度条可视化
- 用量警告提示

### Phase 2: 修改主组件 (3-4小时)

**2.1 修改 AIAssistant.vue**
- 添加三栏布局结构
- 添加状态管理
- 保持非全屏模式不变
- 添加响应式逻辑

**2.2 集成Token统计**
- 从后端API获取Token用量
- 实时更新Token统计
- 添加用量警告逻辑

**2.3 右侧栏显示逻辑**
- 监听智能代理开关
- 监听工具调用事件
- 自动显示/隐藏右侧栏

### Phase 3: 样式优化 (2-3小时)

**3.1 响应式适配**
- 处理窗口大小变化
- 最小宽度限制
- 折叠动画优化

**3.2 主题适配**
- 暗黑模式适配
- 明亮模式适配
- 边框和阴影优化

**3.3 交互优化**
- 折叠/展开动画
- 滑入/滑出动画
- 加载状态处理

### Phase 4: 测试和优化 (2-3小时)

**4.1 功能测试**
- 三栏布局切换
- 对话历史管理
- Token统计准确性
- 工具渲染正确性

**4.2 性能优化**
- 组件懒加载
- 虚拟滚动（对话历史）
- 防抖和节流

**4.3 兼容性测试**
- 不同分辨率测试
- 浏览器兼容性
- 移动端适配（可选）

---

## ⚠️ 注意事项

### 开发规范

1. **不要修改后端代码** ✅
2. **不要修改全局样式** ✅
3. **不要修改工具函数** ✅
4. **只修改前端业务逻辑** ✅

### 兼容性保证

1. **保持非全屏模式不变**
   - 原有侧边栏模式继续工作
   - 拖拽调整宽度功能保留

2. **向后兼容**
   - 现有会话数据不受影响
   - 现有快捷操作继续可用

3. **渐进增强**
   - 先实现基础三栏布局
   - 再添加高级功能

### 性能考虑

1. **组件懒加载**
   ```typescript
   const LeftSidebar = defineAsyncComponent(() => 
     import('./LeftSidebar.vue')
   )
   ```

2. **虚拟滚动**
   - 对话历史列表使用虚拟滚动
   - 消息列表优化渲染

3. **状态管理**
   - 使用 computed 减少重复计算
   - 使用 watchEffect 优化响应式

---

## 📊 预估工作量

| 阶段 | 工作内容 | 预估时间 | 优先级 |
|------|----------|----------|--------|
| Phase 1 | 创建新组件 | 2-3小时 | P0 |
| Phase 2 | 修改主组件 | 3-4小时 | P0 |
| Phase 3 | 样式优化 | 2-3小时 | P1 |
| Phase 4 | 测试优化 | 2-3小时 | P1 |
| **总计** | **完整实现** | **9-13小时** | - |

---

## 🎯 下一步行动

1. ✅ **已完成**: 需求分析和方案设计
2. ⏭️ **下一步**: 创建 LeftSidebar.vue 组件
3. ⏭️ **然后**: 创建 RightSidebar.vue 组件
4. ⏭️ **最后**: 修改 AIAssistant.vue 主组件

---

**文档维护**: AI助手开发团队  
**最后更新**: 2025-10-06

