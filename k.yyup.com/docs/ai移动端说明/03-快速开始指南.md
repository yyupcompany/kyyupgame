# 🚀 幼儿园管理系统 - 快速开始指南

## 📋 文档概述

本指南将帮助您在 15 分钟内搭建完整的幼儿园管理系统开发环境，包含前端、后端、数据库等所有组件的配置和启动。

### 🎯 搭建目标
- ✅ 完整的开发环境搭建
- ✅ 前后端服务正常运行
- ✅ 数据库连接和初始化
- ✅ AI功能正常使用
- ✅ 移动端界面正常访问

## 🛠️ 环境要求

### 系统要求
- **操作系统**: Windows 10+, macOS 10.15+, Ubuntu 18.04+
- **Node.js**: 16.x 或更高版本 (推荐 18.x)
- **MySQL**: 8.0 或更高版本
- **内存**: 8GB+ 推荐
- **存储空间**: 5GB+ 可用空间

### 开发工具
- **代码编辑器**: VS Code (强烈推荐)
- **数据库工具**: MySQL Workbench / Navicat / DBeaver
- **API测试**: Postman / Insomnia
- **浏览器**: Chrome / Firefox (支持开发者工具)

## 📦 快速安装

### 第一步：环境检查

```bash
# 检查Node.js版本
node --version  # 应该是 v16.x 或更高

# 检查npm版本
npm --version   # 应该是 8.x 或更高

# 检查MySQL版本
mysql --version # 应该是 8.0 或更高
```

如果版本不符合要求，请先安装或升级相应软件：

```bash
# 安装Node.js (访问 https://nodejs.org 下载)
# Windows: 下载.msi安装包
# macOS: 下载.pkg安装包或使用 brew install node
# Linux: 使用包管理器 sudo apt install nodejs npm

# 安装MySQL (访问 https://dev.mysql.com/downloads/mysql/)
# 或使用包管理器安装
```

### 第二步：获取项目代码

```bash
# 克隆项目（如果有Git仓库）
git clone [项目仓库地址]
cd lazy-ai-substitute-project

# 或者直接解压项目文件到目标目录
# 确保目录结构如下：
# lazy-ai-substitute-project/
# ├── client/          # 前端代码
# ├── server/          # 后端代码
# ├── database/        # 数据库脚本
# ├── package.json     # 项目配置
# └── README.md
```

### 第三步：一键安装依赖

```bash
# 在项目根目录执行
npm run install:projects

# 或者分步安装
# 后端依赖
cd server
npm install

# 前端依赖
cd ../client
npm install

# 回到根目录
cd ..
```

## 🗄️ 数据库配置

### 第一步：创建数据库

```sql
-- 连接MySQL数据库
mysql -u root -p

-- 创建数据库
CREATE DATABASE ems_ai CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 创建用户（可选，用于生产环境）
CREATE USER 'ems_user'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON ems_ai.* TO 'ems_user'@'localhost';
FLUSH PRIVILEGES;

-- 退出MySQL
EXIT;
```

### 第二步：配置数据库连接

```bash
# 复制环境配置文件
cd server
cp .env.example .env

# 编辑配置文件
# Windows: notepad .env
# macOS/Linux: nano .env 或 vim .env
```

**配置文件内容 (.env)**：

```env
# 服务器配置
PORT=3000
NODE_ENV=development

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=ems_ai
DB_CONNECTION_LIMIT=10

# JWT配置
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# 域名配置
DOMAIN=localhost:5173
FRONTEND_URL=http://localhost:5173

# AI服务配置（可选）
AI_SERVICE_ENABLED=true
AI_MODEL_PROVIDER=local

# 日志配置
LOG_LEVEL=info
LOG_DIR=logs

# 邮件配置（可选）
SMTP_HOST=smtp.qq.com
SMTP_PORT=587
SMTP_USER=your_email@qq.com
SMTP_PASSWORD=your_smtp_password

# 上传配置
UPLOAD_DIR=uploads
MAX_FILE_SIZE=10485760

# Redis配置（可选）
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
```

### 第三步：初始化数据库

```bash
# 在server目录下执行
cd server

# 运行数据库迁移
npm run db:migrate

# 填充初始数据
npm run seed-data:complete

# 或手动执行SQL文件
mysql -u root -p ems_ai < database/migrations/init_tables.sql
mysql -u root -p ems_ai < database/seeds/init_data.sql
```

## 🖥️ 前端配置

### Vite配置

```bash
# 进入前端目录
cd client

# 检查配置文件
cat vite.config.ts
```

**前端配置文件示例**：

```typescript
// client/vite.config.ts
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'

export default defineConfig({
  plugins: [vue()],
  
  resolve: {
    alias: {
      '@': resolve(__dirname, './src'),
    }
  },
  
  server: {
    host: '0.0.0.0',
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:3000',
        changeOrigin: true,
        secure: false
      }
    }
  },
  
  build: {
    outDir: 'dist',
    sourcemap: false,
    minify: 'terser',
    rollupOptions: {
      output: {
        manualChunks: {
          'element-plus': ['element-plus'],
          'vue-vendor': ['vue', 'vue-router', 'pinia'],
          'utils': ['axios', 'dayjs', 'lodash-es']
        }
      }
    }
  }
})
```

### 环境变量配置

```bash
# 创建前端环境配置
touch client/.env.local
```

**前端环境变量 (.env.local)**：

```env
# API地址
VITE_API_BASE_URL=http://localhost:3000/api

# 应用配置
VITE_APP_TITLE=幼儿园管理系统
VITE_APP_VERSION=1.0.0

# 域名配置
VITE_DOMAIN=localhost:5173
VITE_PORT=5173

# AI功能配置
VITE_AI_ENABLED=true
VITE_AI_MODEL=gpt-3.5-turbo

# 调试配置
VITE_DEBUG=true
VITE_LOG_LEVEL=debug
```

## 🚀 启动服务

### 开发环境启动

#### 方式一：一键启动（推荐）

```bash
# 在项目根目录执行
npm start

# 这个命令会同时启动：
# - 后端服务 (localhost:3000)
# - 前端服务 (localhost:5173)
# - 自动打开浏览器
```

#### 方式二：分别启动

```bash
# 终端1 - 启动后端服务
cd server
npm run dev

# 终端2 - 启动前端服务
cd client
npm run dev

# 或者使用localhost:5173域名启动（推荐）
npm run dev:k-yyup
```

#### 方式三：Windows快速启动

```batch
# 双击运行（Windows用户）
start-all.bat

# 或使用PowerShell
.\start-all.ps1
```

### 服务状态检查

```bash
# 检查后端服务
curl http://localhost:3000/api/health
# 预期返回: {"status":"ok","timestamp":"..."}

# 检查前端服务
curl http://localhost:5173
# 预期返回: HTML页面内容

# 检查数据库连接
curl http://localhost:3000/api/database/status
# 预期返回: {"connected":true,"database":"ems_ai"}
```

## 🌐 域名配置（可选）

### 配置本地域名

为了更好的开发体验，项目支持使用 `localhost:5173` 域名进行本地开发：

#### Windows系统

```batch
# 以管理员身份运行命令提示符
# 编辑hosts文件
notepad C:\Windows\System32\drivers\etc\hosts

# 添加以下内容
127.0.0.1 localhost:5173
127.0.0.1 api.localhost:5173
```

#### macOS/Linux系统

```bash
# 编辑hosts文件
sudo nano /etc/hosts

# 添加以下内容
127.0.0.1 localhost:5173
127.0.0.1 api.localhost:5173
```

#### 使用域名启动

```bash
# 前端使用域名启动
cd client
npm run dev:k-yyup

# 访问地址
# http://localhost:5173:5173
```

## 🧪 功能验证

### 1. 基础功能测试

#### 登录功能测试

```bash
# 打开浏览器访问
http://localhost:5173/login
# 或
http://localhost:5173:5173/login

# 使用默认账号登录：
管理员账号: admin / admin123
园长账号: principal / principal123
教师账号: teacher / teacher123
家长账号: parent / parent123
```

#### API接口测试

```bash
# 测试登录接口
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "admin123"
  }'

# 预期返回包含token的JSON响应
```

### 2. AI功能测试

```bash
# 访问AI助手页面
http://localhost:5173/ai

# 测试AI聊天功能
# 1. 点击AI助手菜单
# 2. 输入测试消息："你好，请介绍你的功能"
# 3. 检查是否有正常响应
```

### 3. 移动端响应式测试

```bash
# Chrome开发者工具测试
# 1. 打开 http://localhost:5173
# 2. 按F12打开开发者工具
# 3. 点击设备模拟按钮
# 4. 选择不同设备型号测试
#    - iPhone 12 Pro (390 x 844)
#    - iPad Air (820 x 1180)
#    - Galaxy S20 (360 x 800)
```

### 4. 权限系统测试

```bash
# 测试不同角色权限
# 1. 使用不同角色账号登录
# 2. 检查侧边栏菜单差异
# 3. 尝试访问无权限页面
# 4. 验证权限控制是否正常
```

## 🛠️ 开发工具配置

### VS Code配置

```json
// .vscode/settings.json
{
  "typescript.preferences.quoteStyle": "single",
  "editor.formatOnSave": true,
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  },
  "eslint.workingDirectories": ["client", "server"],
  "files.associations": {
    "*.vue": "vue"
  },
  "vue.codeActions.enabled": true,
  "vue.complete.casing.props": "kebab"
}
```

```json
// .vscode/extensions.json
{
  "recommendations": [
    "vue.volar",
    "vue.vscode-typescript-vue-plugin",
    "ms-vscode.vscode-typescript-next",
    "esbenp.prettier-vscode",
    "dbaeumer.vscode-eslint",
    "formulahendry.auto-rename-tag",
    "christian-kohler.path-intellisense"
  ]
}
```

### Git配置

```bash
# 设置Git用户信息
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

# 设置默认分支名
git config --global init.defaultBranch main

# 设置换行符处理
git config --global core.autocrlf input  # macOS/Linux
git config --global core.autocrlf true   # Windows
```

## 📱 移动端开发配置

### 移动端调试

```bash
# 启用移动端调试模式
cd client
npm run dev:mobile

# 或使用网络调试
npm run dev -- --host 0.0.0.0

# 通过IP地址在手机上访问
# http://你的IP地址:5173
```

### PWA配置（可选）

```typescript
// client/vite.config.ts - 添加PWA插件
import { VitePWA } from 'vite-plugin-pwa'

export default defineConfig({
  plugins: [
    vue(),
    VitePWA({
      registerType: 'autoUpdate',
      workbox: {
        globPatterns: ['**/*.{js,css,html,ico,png,svg}']
      },
      manifest: {
        name: '幼儿园管理系统',
        short_name: '幼儿园管理',
        description: '专业的幼儿园管理系统',
        theme_color: '#1890ff',
        background_color: '#ffffff',
        display: 'standalone',
        scope: '/',
        start_url: '/',
        icons: [
          {
            src: '/icons/icon-192.png',
            sizes: '192x192',
            type: 'image/png'
          }
        ]
      }
    })
  ]
})
```

## 🐛 常见问题解决

### 问题1: 端口冲突

```bash
# 症状：EADDRINUSE: address already in use :::3000
# 解决方案1：杀死占用端口的进程
# Windows:
netstat -ano | findstr :3000
taskkill /PID <PID号> /F

# macOS/Linux:
lsof -ti:3000 | xargs kill -9

# 解决方案2：使用不同端口
# 修改server/.env中的PORT=3001
# 修改client/vite.config.ts中的proxy target
```

### 问题2: 数据库连接失败

```bash
# 症状：ECONNREFUSED localhost:3306
# 检查MySQL服务状态
# Windows:
sc query mysql80

# macOS:
brew services list | grep mysql

# Linux:
systemctl status mysql

# 启动MySQL服务
# Windows:
net start mysql80

# macOS:
brew services start mysql

# Linux:
sudo systemctl start mysql
```

### 问题3: 前端构建失败

```bash
# 症状：Module not found或TypeScript错误
# 解决方案：清理缓存重新安装
cd client
rm -rf node_modules package-lock.json
npm cache clean --force
npm install

# 检查Node.js版本
node --version  # 确保是16+

# 清理Vite缓存
npx vite --force
```

### 问题4: AI功能不可用

```bash
# 症状：AI助手无响应或报错
# 检查后端AI服务配置
curl http://localhost:3000/api/ai/status

# 检查环境变量
cat server/.env | grep AI_

# 查看后端日志
cd server
npm run logs
```

### 问题5: 权限问题

```bash
# 症状：登录后看不到菜单或页面403
# 检查数据库权限数据
mysql -u root -p ems_ai
SELECT * FROM permissions LIMIT 10;
SELECT * FROM roles LIMIT 10;
SELECT * FROM role_permissions LIMIT 10;

# 重新初始化权限数据
npm run seed-data:permissions
```

## 🎯 性能优化

### 开发环境优化

```json
// package.json - 添加性能优化脚本
{
  "scripts": {
    "dev:fast": "NODE_OPTIONS='--max-old-space-size=8192' npm run dev",
    "dev:turbo": "NODE_OPTIONS='--max-old-space-size=8192' UV_THREADPOOL_SIZE=12 npm run dev",
    "build:analyze": "npm run build && npx vite-bundle-analyzer dist"
  }
}
```

### 构建优化

```typescript
// vite.config.ts - 生产构建优化
export default defineConfig({
  build: {
    minify: 'terser',
    terserOptions: {
      compress: {
        drop_console: true,
        drop_debugger: true
      }
    },
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['vue', 'vue-router', 'pinia'],
          elementPlus: ['element-plus'],
          utils: ['axios', 'dayjs', 'lodash-es']
        }
      }
    }
  }
})
```

## 📊 监控和日志

### 开发环境监控

```bash
# 查看实时日志
# 后端日志
cd server && npm run logs

# 前端日志
cd client && npm run logs

# 数据库连接监控
curl http://localhost:3000/api/health/database
```

### 性能监控

```bash
# 安装性能监控工具
npm install -g clinic

# 分析Node.js性能
cd server
clinic doctor -- npm run dev

# 分析前端性能
cd client
npm run build:analyze
```

## 🎉 成功验证

完成以上步骤后，你应该能够：

### ✅ 基础功能验证
- [ ] 前端服务运行在 http://localhost:5173
- [ ] 后端服务运行在 http://localhost:3000
- [ ] 数据库连接正常
- [ ] 能够正常登录系统

### ✅ 界面功能验证
- [ ] 响应式布局在不同设备上正常显示
- [ ] 侧边栏菜单可以展开收起
- [ ] 不同角色看到不同的菜单内容
- [ ] AI助手页面可以正常访问

### ✅ API功能验证
- [ ] 登录接口返回正确的token
- [ ] 权限接口返回正确的权限列表
- [ ] CRUD操作接口正常工作
- [ ] 文件上传功能正常

### ✅ 开发环境验证
- [ ] 热更新（HMR）正常工作
- [ ] TypeScript类型检查正常
- [ ] ESLint代码检查正常
- [ ] 浏览器开发者工具无错误

## 🔄 下一步行动

环境搭建完成后，建议按以下顺序进行开发：

1. **🎨 界面开发** - 查看 [组件开发指南](./04-组件开发指南.md)
2. **🔌 API集成** - 查看 [API集成说明](./05-API集成说明.md)  
3. **📱 移动端优化** - 查看 [最佳实践](./06-最佳实践.md)
4. **🧪 测试和部署** - 查看项目根目录下的部署文档

## 🆘 获取帮助

如果在搭建过程中遇到问题：

1. **📖 查看日志** - 检查控制台输出和错误信息
2. **🔍 搜索文档** - 在项目文档中搜索相关问题
3. **💬 社区支持** - 在GitHub Issues中提问
4. **🛠️ 技术支持** - 联系项目维护者

---

**🎉 恭喜！** 您已成功搭建了幼儿园管理系统的开发环境。

**下一步推荐**: 阅读 [组件开发指南](./04-组件开发指南.md) 开始界面开发。

*文档版本: v1.0.0*  
*更新时间: 2025-08-10*  
*基于实际项目搭建流程整理*