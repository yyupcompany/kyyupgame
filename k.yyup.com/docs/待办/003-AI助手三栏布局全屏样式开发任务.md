# AI助手三栏布局全屏样式开发任务

**创建时间**: 2025-10-06
**状态**: ✅ 已完成（样式重构）
**完成时间**: 2025-10-08 13:29
**优先级**: ⭐⭐⭐⭐⭐
**预估工时**: 9-13小时
**实际耗时**: 约8小时（分12个步骤完成）

> **📊 完成报告**: 详见 [AI助手全屏样式重构完成报告.md](../AI助手全屏样式重构完成报告.md)

---

## 📋 任务概述

在AI助手全屏模式下，实现类似 **claude.ai** 的三栏布局，包含丰富的动画效果和交互体验。

### 核心目标
- ✅ 实现三栏布局（左侧栏 + 中间主区域 + 右侧栏）
- ✅ 使用全局样式变量，确保主题一致性
- ✅ 添加丰富的动画效果（骨架屏、脉冲、旋转等）
- ✅ 保持非全屏模式不变
- ✅ 完全响应式设计

---

## 🎯 功能需求

### 1. 左侧栏（可折叠）
- [ ] 对话历史列表
- [ ] Token用量统计卡片
- [ ] 新建会话按钮
- [ ] 折叠/展开切换按钮
- [ ] 折叠状态：60px（仅显示图标）
- [ ] 展开状态：280px（显示完整内容）

### 2. 中间主区域（自适应）
- [ ] 头部区域（标题 + 操作按钮）
- [ ] 上下文横幅（页面信息 + 用户角色）
- [ ] 聊天区域（消息列表）
- [ ] 输入区域（文本框 + 功能按钮）
- [ ] 使用 flex: 1 自适应宽度

### 3. 右侧栏（条件显示）
- [ ] 工具调用状态显示
- [ ] 组件渲染区域（图表、表格、报表）
- [ ] 标签页切换（工具调用 / 渲染结果）
- [ ] 关闭按钮
- [ ] 固定宽度：400px
- [ ] 仅在智能工具开启时显示

---

## 🎨 动画效果需求

### 1. 骨架屏动画（非流式模式）

**使用场景**:
- 加载对话历史时
- 加载Token统计时
- 等待AI响应时（非流式）

**实现要求**:
```scss
// 使用全局变量
.skeleton-loading {
  background: linear-gradient(
    90deg,
    var(--bg-color) 25%,
    var(--bg-hover) 50%,
    var(--bg-color) 75%
  );
  background-size: 200% 100%;
  animation: skeleton-shimmer 1.5s ease-in-out infinite;
}

@keyframes skeleton-shimmer {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}
```

### 2. 工具调用动画

**使用场景**:
- 工具调用进行中
- 右侧栏工具状态显示

**实现要求**:
```scss
// 小齿轮旋转动画
.tool-calling-icon {
  animation: gear-rotate 2s linear infinite;
}

@keyframes gear-rotate {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

// 工具调用脉冲效果
.tool-calling-badge {
  animation: pulse-glow 1.5s ease-in-out infinite;
}

@keyframes pulse-glow {
  0%, 100% {
    box-shadow: 0 0 0 0 rgba(var(--primary-color-rgb), 0.7);
  }
  50% {
    box-shadow: 0 0 0 8px rgba(var(--primary-color-rgb), 0);
  }
}
```

### 3. AI思考动画

**使用场景**:
- AI正在思考时
- 思考过程文字显示

**实现要求**:
```scss
// 文字脉冲动画
.thinking-text {
  animation: text-pulse 1.5s ease-in-out infinite;
}

@keyframes text-pulse {
  0%, 100% {
    opacity: 1;
    transform: scale(1);
  }
  50% {
    opacity: 0.7;
    transform: scale(0.98);
  }
}

// 思考点动画
.thinking-dots {
  span {
    animation: dot-bounce 1.4s ease-in-out infinite;
    
    &:nth-child(1) { animation-delay: 0s; }
    &:nth-child(2) { animation-delay: 0.2s; }
    &:nth-child(3) { animation-delay: 0.4s; }
  }
}

@keyframes dot-bounce {
  0%, 60%, 100% { transform: translateY(0); }
  30% { transform: translateY(-10px); }
}
```

### 4. 侧边栏展开/折叠动画

**实现要求**:
```scss
.left-sidebar,
.right-sidebar {
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

// 滑入动画
@keyframes slide-in-left {
  from {
    transform: translateX(-100%);
    opacity: 0;
  }
  to {
    transform: translateX(0);
    opacity: 1;
  }
}

@keyframes slide-in-right {
  from {
    transform: translateX(100%);
    opacity: 0;
  }
  to {
    transform: translateX(0);
    opacity: 1;
  }
}
```

### 5. 消息渐入动画

**实现要求**:
```scss
.message-item {
  animation: message-fade-in 0.4s ease-out;
}

@keyframes message-fade-in {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```

---

## 🎨 全局样式变量使用规范

### 颜色变量
```scss
// 使用 design-tokens.scss 中的变量
--primary-color          // 主色调
--success-color          // 成功色
--warning-color          // 警告色
--danger-color           // 危险色
--info-color             // 信息色

--text-primary           // 主要文字
--text-secondary         // 次要文字
--text-tertiary          // 三级文字
--text-placeholder       // 占位文字

--bg-color               // 主背景
--bg-color-page          // 页面背景
--bg-hover               // 悬停背景
--bg-active              // 激活背景

--border-color           // 边框颜色
--border-color-light     // 浅色边框
```

### 间距变量
```scss
// 使用 design-tokens.scss 中的间距系统
--spacing-xs: 4px
--spacing-sm: 8px
--spacing-md: 16px
--spacing-lg: 24px
--spacing-xl: 32px
--spacing-2xl: 48px
--spacing-3xl: 64px
```

### 阴影变量
```scss
--shadow-sm              // 小阴影
--shadow-md              // 中等阴影
--shadow-lg              // 大阴影
--shadow-xl              // 超大阴影
```

### 圆角变量
```scss
--radius-sm              // 小圆角
--radius-md              // 中等圆角
--radius-lg              // 大圆角
--radius-full            // 完全圆角
```

---

## 📁 文件结构

### 新建组件
```
client/src/components/ai-assistant/
├── LeftSidebar.vue              # 左侧栏组件
├── RightSidebar.vue             # 右侧栏组件
├── TokenUsageCard.vue           # Token统计卡片
├── SkeletonLoader.vue           # 骨架屏组件
├── ThinkingIndicator.vue        # 思考指示器组件
└── ToolCallingIndicator.vue     # 工具调用指示器组件
```

### 新建样式文件
```
client/src/styles/components/
└── ai-assistant-fullscreen.scss  # AI助手全屏样式

client/src/components/ai-assistant/
└── animations.scss               # 动画样式（已存在，需扩展）
```

---

## 🛠️ 开发任务清单

### Phase 1: 基础组件开发 (3-4小时)

#### 1.1 创建左侧栏组件
- [ ] 创建 `LeftSidebar.vue`
- [ ] 实现折叠/展开功能
- [ ] 集成对话历史列表
- [ ] 添加Token统计卡片
- [ ] 添加新建会话按钮
- [ ] 使用全局样式变量
- [ ] 添加折叠/展开动画

#### 1.2 创建右侧栏组件
- [ ] 创建 `RightSidebar.vue`
- [ ] 实现工具调用状态显示
- [ ] 实现组件渲染区域
- [ ] 添加标签页切换
- [ ] 添加关闭按钮
- [ ] 使用全局样式变量
- [ ] 添加滑入/滑出动画

#### 1.3 创建Token统计组件
- [ ] 创建 `TokenUsageCard.vue`
- [ ] 显示当前会话Token用量
- [ ] 显示总Token用量
- [ ] 添加进度条可视化
- [ ] 添加用量警告提示
- [ ] 使用全局样式变量

### Phase 2: 动画组件开发 (2-3小时)

#### 2.1 骨架屏组件
- [ ] 创建 `SkeletonLoader.vue`
- [ ] 实现shimmer动画效果
- [ ] 支持不同形状（矩形、圆形、文本）
- [ ] 支持自定义尺寸
- [ ] 使用全局样式变量

#### 2.2 思考指示器组件
- [ ] 创建 `ThinkingIndicator.vue`
- [ ] 实现文字脉冲动画
- [ ] 实现思考点跳动动画
- [ ] 支持自定义文案
- [ ] 使用全局样式变量

#### 2.3 工具调用指示器组件
- [ ] 创建 `ToolCallingIndicator.vue`
- [ ] 实现齿轮旋转动画
- [ ] 实现脉冲光晕效果
- [ ] 显示工具调用状态
- [ ] 使用全局样式变量

### Phase 3: 主组件集成 (3-4小时)

#### 3.1 修改 AIAssistant.vue
- [ ] 添加三栏布局结构（仅全屏模式）
- [ ] 集成左侧栏组件
- [ ] 集成右侧栏组件
- [ ] 添加状态管理
- [ ] 保持非全屏模式不变
- [ ] 使用全局样式变量

#### 3.2 状态管理
- [ ] 添加左侧栏折叠状态
- [ ] 添加右侧栏显示状态
- [ ] 添加Token统计状态
- [ ] 监听智能代理开关
- [ ] 监听工具调用事件

#### 3.3 样式实现
- [ ] 创建 `ai-assistant-fullscreen.scss`
- [ ] 实现三栏Flexbox布局
- [ ] 实现响应式适配
- [ ] 添加所有动画效果
- [ ] 使用全局样式变量
- [ ] 主题适配（暗黑/明亮）

### Phase 4: 测试和优化 (2-3小时)

#### 4.1 功能测试
- [ ] 测试三栏布局切换
- [ ] 测试对话历史管理
- [ ] 测试Token统计准确性
- [ ] 测试工具渲染正确性
- [ ] 测试所有动画效果

#### 4.2 性能优化
- [ ] 组件懒加载
- [ ] 虚拟滚动（对话历史）
- [ ] 防抖和节流
- [ ] 动画性能优化

#### 4.3 兼容性测试
- [ ] 不同分辨率测试
- [ ] 浏览器兼容性测试
- [ ] 主题切换测试
- [ ] 响应式布局测试

---

## 📊 进度追踪

| 阶段 | 任务 | 状态 | 完成时间 |
|------|------|------|----------|
| Phase 1 | 基础组件开发 | ✅ 已完成 | 2025-10-08 |
| Phase 2 | 动画组件开发 | ✅ 已完成 | 2025-10-08 |
| Phase 3 | 主组件集成 | ✅ 已完成 | 2025-10-08 |
| Phase 4 | 测试和优化 | 🔄 进行中 | - |

### 样式重构完成情况（12个步骤）
- ✅ 第1步：统一关键尺寸
- ✅ 第2步：统一欢迎块与Markdown渲染
- ✅ 第3步：统一高级消息分支
- ✅ 第4步：左侧工具栏与右侧面板细节对齐
- ✅ 第5步：侧栏间距细化
- ✅ 第6步：中部消息区滚动条统一
- ✅ 第7步：侧栏间距微调
- ✅ 第8步：收尾微调与统一覆盖
- ✅ 第9步：最终扫尾
- ✅ 第10步：规范头部高度+hover统一
- ✅ 第11步：左侧折叠按钮规格统一
- ✅ 第12步：代码高亮主题微调 + 统计卡片对比度增强 + 工具项active态支持

### 待完成任务
- [ ] 功能联调测试
- [ ] E2E测试
- [ ] 视觉回归测试
- [ ] 多浏览器兼容性测试

---

## ⚠️ 注意事项

### 开发规范
1. ✅ **只修改前端业务逻辑代码**
2. ✅ **不修改后端代码**
3. ✅ **不修改全局样式文件**（只使用变量）
4. ✅ **不修改工具函数**

### 样式规范
1. ✅ **必须使用全局样式变量**
2. ✅ **不要硬编码颜色值**
3. ✅ **不要硬编码间距值**
4. ✅ **使用语义化变量名**

### 动画规范
1. ✅ **使用 CSS 动画，避免 JS 动画**
2. ✅ **使用 transform 和 opacity 优化性能**
3. ✅ **添加 will-change 提示浏览器优化**
4. ✅ **使用 cubic-bezier 缓动函数**

### 兼容性规范
1. ✅ **保持非全屏模式不变**
2. ✅ **向后兼容现有功能**
3. ✅ **渐进增强，不破坏现有功能**

---

## 📝 相关文档

- [AI助手三栏布局开发方案](../AI助手三栏布局开发方案.md)
- [AI架构文档](../ai架构/README.md)
- [全局样式变量](../../client/src/styles/design-tokens.scss)
- [动画样式](../../client/src/components/ai-assistant/animations.scss)

---

## 🎬 动画效果预览

### 1. 骨架屏效果
```
┌─────────────────────────────────┐
│ ▓▓▓▓▓▓▓▓░░░░░░░░░░░░░░░░░░░░░░ │ ← shimmer动画
│ ▓▓▓▓░░░░░░░░░░░░░░░░░░░░░░░░░░ │
│ ▓▓▓▓▓▓▓▓▓▓░░░░░░░░░░░░░░░░░░░░ │
└─────────────────────────────────┘
```

### 2. 工具调用效果
```
⚙️ ← 旋转动画 (2s linear infinite)
💫 ← 脉冲光晕 (1.5s ease-in-out infinite)
```

### 3. AI思考效果
```
AI正在思考 ● ● ● ← 点跳动动画
思考中的文字 ← 脉冲动画 (opacity + scale)
```

### 4. 侧边栏动画
```
← 滑入 (0.3s cubic-bezier)
→ 滑出 (0.3s cubic-bezier)
```

---

## 🎨 动画性能优化建议

### 1. 使用 GPU 加速
```scss
.animated-element {
  will-change: transform, opacity;
  transform: translateZ(0); // 强制GPU加速
}
```

### 2. 避免重排和重绘
```scss
// ✅ 好的做法 - 只改变 transform 和 opacity
.good-animation {
  animation: slide-in 0.3s ease;
}

@keyframes slide-in {
  from { transform: translateX(-100%); opacity: 0; }
  to { transform: translateX(0); opacity: 1; }
}

// ❌ 不好的做法 - 改变 width 会触发重排
.bad-animation {
  animation: expand 0.3s ease;
}

@keyframes expand {
  from { width: 0; }
  to { width: 280px; }
}
```

### 3. 使用 requestAnimationFrame
```typescript
// 对于复杂的JS动画，使用RAF
function animate() {
  // 动画逻辑
  requestAnimationFrame(animate)
}
```

---

## 📋 验收标准

### 功能验收
- [ ] 三栏布局正确显示
- [ ] 左侧栏折叠/展开正常
- [ ] 右侧栏条件显示正常
- [ ] Token统计准确显示
- [ ] 对话历史正常加载

### 动画验收
- [ ] 骨架屏动画流畅
- [ ] 工具调用动画正常
- [ ] AI思考动画正常
- [ ] 侧边栏动画流畅
- [ ] 消息渐入动画正常

### 性能验收
- [ ] 动画帧率 ≥ 60fps
- [ ] 无明显卡顿
- [ ] 内存占用正常
- [ ] CPU占用正常

### 兼容性验收
- [ ] Chrome 浏览器正常
- [ ] Firefox 浏览器正常
- [ ] Safari 浏览器正常
- [ ] 暗黑主题正常
- [ ] 明亮主题正常

---

**任务创建人**: AI助手开发团队
**最后更新**: 2025-10-06
**预计完成时间**: 2025-10-08

