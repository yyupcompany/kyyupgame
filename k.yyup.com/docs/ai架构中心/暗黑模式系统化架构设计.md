# 暗黑模式系统化架构设计

## 📐 整体布局架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                    1️⃣ 头部导航 (Header Navigation)                   │
│  Logo + 标题 + 连接状态 | 统计 工具 清空 主题 退出                      │
├──────────┬──────────────────────────────────────────┬──────────────┤
│          │                                          │              │
│   2️⃣     │          3️⃣ 中间对话区域                  │     4️⃣      │
│  左侧    │    ┌──────────────────────┐              │    右侧     │
│  专家    │    │   欢迎消息卡片        │              │    工具     │
│  面板    │    │   (WelcomeMessage)   │              │    面板     │
│          │    └──────────────────────┘              │             │
│  可折叠  │    ┌──────────────────────┐              │   可折叠    │
│  280px   │    │   消息列表            │              │   400px     │
│  ↓       │    │   (MessageList)      │              │    ↓        │
│  60px    │    │   - MessageItem      │              │   隐藏      │
│          │    │   - MessageItem      │              │             │
│          │    └──────────────────────┘              │             │
│          │                                          │             │
│          │    ┌──────────────────────┐              │             │
│          │    │  5️⃣ 输入框 (InputArea) │              │             │
│          │    └──────────────────────┘              │             │
└──────────┴──────────────────────────────────────────┴──────────────┘
```

---

## 🎨 五大区域详细设计

### 1️⃣ 头部导航区域 (Header Navigation)

**组件**: `FullscreenLayout.vue` → `.center-header`

**设计原则**:
- 固定高度，不随内容变化
- 玻璃态半透明背景
- 操作按钮统一风格

**主题变量**:
```scss
// 浅色模式
--ai-header-bg: rgba(255, 255, 255, 0.95);
--ai-header-border: rgba(139, 92, 246, 0.15);
--ai-header-shadow: rgba(0, 0, 0, 0.05);

// 暗黑模式
--ai-header-bg: rgba(30, 41, 59, 0.95);
--ai-header-border: rgba(139, 92, 246, 0.25);
--ai-header-shadow: rgba(0, 0, 0, 0.3);
```

**样式实现**:
```scss
.center-header {
  background: var(--ai-header-bg);
  border-bottom: 1px solid var(--ai-header-border);
  backdrop-filter: blur(20px) saturate(180%);
  box-shadow: 0 1px 4px var(--ai-header-shadow);
}
```

**操作按钮**:
```scss
.action-btn {
  background: var(--ai-header-btn-bg);
  border: 1px solid var(--ai-header-btn-border);
  color: var(--ai-header-btn-color);
  
  &:hover {
    background: var(--ai-header-btn-hover-bg);
    border-color: var(--ai-header-btn-hover-border);
    color: var(--ai-header-btn-hover-color);
  }
}
```

---

### 2️⃣ 左侧专家面板 (Expert Selector)

**组件**: `ExpertSelector.vue`

**状态**:
- 展开: 280px
- 折叠: 60px

**主题变量** (预留):
```scss
--ai-left-sidebar-bg: rgba(248, 250, 252, 0.95);  // 浅色
--ai-left-sidebar-bg: rgba(15, 23, 42, 0.95);     // 暗黑

--ai-expert-item-bg: rgba(255, 255, 255, 0.9);    // 浅色
--ai-expert-item-bg: rgba(30, 41, 59, 0.8);       // 暗黑
```

**响应式影响**:
- 折叠时: 对话框宽度增加到 85%
- 展开时: 对话框宽度保持 80%

---

### 3️⃣ 中间对话区域 (Chat Area)

**组件结构**:
```
ChatContainer
  ├── WelcomeMessage (欢迎卡片)
  └── MessageList (消息列表)
       └── MessageItem (单条消息)
```

#### 3.1 欢迎消息卡片 (WelcomeMessage)

**主题变量**:
```scss
// 浅色模式
--ai-welcome-bg-start: rgba(249, 250, 251, 0.95);
--ai-welcome-bg-end: rgba(243, 244, 246, 0.9);
--ai-welcome-border: rgba(139, 92, 246, 0.2);
--ai-welcome-shadow: rgba(0, 0, 0, 0.05);

// 暗黑模式
--ai-welcome-bg-start: rgba(30, 41, 59, 0.8);
--ai-welcome-bg-end: rgba(15, 23, 42, 0.7);
--ai-welcome-border: rgba(139, 92, 246, 0.3);
--ai-welcome-shadow: rgba(0, 0, 0, 0.4);
```

**样式实现**:
```scss
.welcome-card {
  background: linear-gradient(135deg, 
    var(--ai-welcome-bg-start) 0%, 
    var(--ai-welcome-bg-end) 100%
  );
  border: 1px solid var(--ai-welcome-border);
  box-shadow: 0 2px 12px var(--ai-welcome-shadow);
  backdrop-filter: blur(20px) saturate(180%);
}
```

#### 3.2 消息列表 (MessageList)

**宽度控制**:
```scss
.message-list {
  width: var(--ai-content-width);
  max-width: var(--ai-content-max-width);
  transition: all var(--ai-transition-normal);
}
```

#### 3.3 聊天容器 (ChatContainer)

**背景和滚动条**:
```scss
.chat-messages {
  background: var(--ai-chat-bg);
  
  &::-webkit-scrollbar-thumb {
    background: var(--ai-scrollbar-thumb);
  }
  
  &::-webkit-scrollbar-thumb:hover {
    background: var(--ai-scrollbar-thumb-hover);
  }
}
```

---

### 4️⃣ 右侧工具面板 (Right Sidebar)

**组件**: `RightSidebar.vue`

**状态**:
- 展开: 400px
- 隐藏: 0px

**主题变量** (预留):
```scss
--ai-right-sidebar-bg: rgba(248, 250, 252, 0.95);  // 浅色
--ai-right-sidebar-bg: rgba(15, 23, 42, 0.95);     // 暗黑

--ai-tool-item-bg: rgba(255, 255, 255, 0.9);       // 浅色
--ai-tool-item-bg: rgba(30, 41, 59, 0.8);          // 暗黑
```

**响应式影响**:
- 展开时: 对话框宽度减少到 75%
- 隐藏时: 对话框宽度保持 80%

---

### 5️⃣ 输入框区域 (Input Area)

**组件**: `InputArea.vue`

**主题变量**:
```scss
// 浅色模式
--ai-input-bg-start: rgba(255, 255, 255, 0.95);
--ai-input-bg-end: rgba(248, 250, 252, 0.9);
--ai-input-border: rgba(139, 92, 246, 0.25);
--ai-input-shadow: rgba(0, 0, 0, 0.08);

// 暗黑模式
--ai-input-bg-start: rgba(30, 41, 59, 0.8);
--ai-input-bg-end: rgba(15, 23, 42, 0.7);
--ai-input-border: rgba(139, 92, 246, 0.3);
--ai-input-shadow: rgba(0, 0, 0, 0.4);
```

**样式实现**:
```scss
.input-wrapper {
  background: linear-gradient(135deg, 
    var(--ai-input-bg-start) 0%, 
    var(--ai-input-bg-end) 100%
  );
  border: 1.5px solid var(--ai-input-border);
  box-shadow: 0 8px 32px var(--ai-input-shadow);
  backdrop-filter: blur(20px) saturate(180%);
  
  width: var(--ai-content-width) !important;
  max-width: var(--ai-content-max-width) !important;
}
```

---

## 🔧 响应式宽度管理系统

### 核心变量
```scss
:root {
  --ai-content-width: min(80%, 900px);
  --ai-content-max-width: 900px;
}
```

### 自动调整规则

#### 规则1: 右侧工具栏展开
```scss
.ai-assistant-fullscreen:has(.right-sidebar.visible) {
  --ai-content-width: min(75%, 800px);
  --ai-content-max-width: 800px;
}
```

#### 规则2: 左侧专家面板折叠
```scss
.ai-assistant-fullscreen:has(.expert-selector.collapsed) {
  --ai-content-width: min(85%, 1000px);
  --ai-content-max-width: 1000px;
}
```

#### 规则3: 两侧都展开
```scss
.ai-assistant-fullscreen:has(.expert-selector:not(.collapsed)):has(.right-sidebar.visible) {
  --ai-content-width: min(70%, 750px);
  --ai-content-max-width: 750px;
}
```

#### 规则4: 两侧都折叠
```scss
.ai-assistant-fullscreen:has(.expert-selector.collapsed):has(.right-sidebar:not(.visible)) {
  --ai-content-width: min(90%, 1200px);
  --ai-content-max-width: 1200px;
}
```

### 宽度变化流程图

```
两侧都展开 → 70%, 750px  (最窄)
     ↓
右侧展开   → 75%, 800px
     ↓
默认状态   → 80%, 900px  (标准)
     ↓
左侧折叠   → 85%, 1000px
     ↓
两侧都折叠 → 90%, 1200px (最宽)
```

---

## 📱 响应式断点

### 大屏 (>1440px)
```scss
:root {
  --ai-content-width: min(80%, 900px);
}
```

### 中屏 (1024px - 1440px)
```scss
@media (max-width: 1440px) {
  :root {
    --ai-content-width: min(85%, 800px);
  }
}
```

### 小屏 (768px - 1024px)
```scss
@media (max-width: 1024px) {
  :root {
    --ai-content-width: min(90%, 700px);
  }
}
```

### 移动端 (<768px)
```scss
@media (max-width: 768px) {
  :root {
    --ai-content-width: 95%;
  }
}
```

---

## 🎨 主题切换效果对比

### 浅色模式 (Light Theme)
| 区域 | 背景色 | 边框色 | 文字色 |
|------|--------|--------|--------|
| 头部 | rgba(255,255,255,0.95) | rgba(139,92,246,0.15) | #1f2937 |
| 欢迎卡片 | 浅灰渐变 | rgba(139,92,246,0.2) | #1f2937 |
| 输入框 | 白色渐变 | rgba(139,92,246,0.25) | #1f2937 |
| 全局背景 | #f9fafb → #ffffff | - | - |

### 暗黑模式 (Dark Theme)
| 区域 | 背景色 | 边框色 | 文字色 |
|------|--------|--------|--------|
| 头部 | rgba(30,41,59,0.95) | rgba(139,92,246,0.25) | #f3f4f6 |
| 欢迎卡片 | 深色渐变 | rgba(139,92,246,0.3) | #f3f4f6 |
| 输入框 | 深蓝渐变 | rgba(139,92,246,0.3) | #f3f4f6 |
| 全局背景 | #0f172a → #1e293b | - | - |

---

## ✨ 架构优势

| 优势 | 说明 | 实现方式 |
|------|------|----------|
| **统一管理** | 所有颜色集中管理 | theme-variables.scss |
| **自动适配** | 主题切换自动响应 | CSS变量 + :root |
| **响应式宽度** | 侧边栏状态自动调整 | :has() 选择器 |
| **易于维护** | 修改主题只需改变变量 | 单一数据源 |
| **性能优化** | 运行时切换，无需编译 | 纯CSS实现 |
| **可扩展性** | 轻松添加新主题 | 变量系统 |

---

## 📝 使用指南

### 1. 主题切换
点击头部的主题按钮（太阳/月亮图标）即可切换

### 2. 宽度调整
- 展开/折叠左侧专家面板
- 展开/隐藏右侧工具面板
- 对话框和输入框自动调整宽度

### 3. 自定义主题
修改 `theme-variables.scss` 中的变量值：
```scss
[data-theme="dark"] {
  --ai-header-bg: rgba(30, 41, 59, 0.95);  // 修改这里
}
```

---

## 🔍 技术细节

### CSS变量继承
```scss
// 父级定义
:root {
  --ai-content-width: min(80%, 900px);
}

// 子组件使用
.welcome-message {
  width: var(--ai-content-width);
}
```

### 玻璃态效果
```scss
backdrop-filter: blur(20px) saturate(180%);
```

### 平滑过渡
```scss
transition: all var(--ai-transition-normal);  // 0.3s
```

---

**文档版本**: v1.0  
**最后更新**: 2025-10-12  
**维护者**: AI助手团队

