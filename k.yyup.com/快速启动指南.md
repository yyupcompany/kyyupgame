# 🚀 Docker 快速启动指南

## 一键启动

### Windows系统
```cmd
# 方法1：使用 Docker Compose
docker-compose up -d

# 方法2：使用构建脚本（需要Git Bash或WSL）
bash docker/docker-build.sh
```

### Linux/Mac系统
```bash
# 方法1：使用 Docker Compose
docker-compose up -d

# 方法2：使用构建脚本
./docker/docker-build.sh
```

## 访问应用

启动成功后，在浏览器中访问：
- **主应用**: http://localhost
- **API文档**: http://localhost/api （如果有配置）

## 常用命令

```bash
# 查看运行状态
docker-compose ps

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down

# 重启服务
docker-compose restart

# 更新并重启
docker-compose build --no-cache
docker-compose up -d --force-recreate
```

## 故障排除

### 端口被占用
```bash
# Windows
netstat -ano | findstr :80
netstat -ano | findstr :3000

# Linux/Mac
lsof -i :80
lsof -i :3000
```

### 清理Docker
```bash
# 清理未使用的镜像
docker image prune -f

# 清理所有未使用的资源
docker system prune -a
```

### 查看详细日志
```bash
# 进入容器查看
docker exec -it kindergarten-system sh

# 查看supervisor状态
supervisorctl status

# 查看nginx配置
nginx -t
```

## 开发模式

如需修改代码并实时看到效果：

1. 停止Docker容器
2. 本地启动开发服务器：
   ```bash
   # 启动后端（在server目录）
   npm run dev
   
   # 启动前端（在client目录）  
   npm run dev
   ```

## 数据备份

重要数据目录已映射到宿主机：
- 上传文件: `./uploads`
- 日志文件: `./server/logs`

## 技术支持

遇到问题请检查：
1. Docker版本是否为20.10+
2. Docker Compose版本是否为2.0+
3. 可用内存是否大于2GB
4. 磁盘空间是否充足