# 海报预览组件修复方案

## 问题定位

**文件**: `unified-tenant-system/client/src/components/preview/PosterPreview.vue`

**问题**: 组件只显示基础信息，缺少活动关键信息

---

## 修复方案

### 第一步：扩展 Props 定义

```typescript
interface Props {
  content: string
  theme?: string
  schoolName?: string
  logoUrl?: string
  phone?: string
  address?: string
  showQR?: boolean
  
  // 新增：活动信息
  activityTitle?: string      // 活动标题
  activityTime?: string       // 活动时间 (格式: "2024-01-15 14:00 - 16:00")
  location?: string           // 活动地点
  capacity?: number           // 参与人数
  fee?: number               // 活动费用
  agenda?: string            // 活动议程
}
```

### 第二步：修改模板显示

在 `main-content` 区域添加活动信息卡片：

```vue
<!-- 活动关键信息 -->
<div class="activity-info" v-if="activityTitle">
  <div class="info-item">
    <span class="icon">📅</span>
    <span class="text">{{ activityTime }}</span>
  </div>
  <div class="info-item">
    <span class="icon">📍</span>
    <span class="text">{{ location }}</span>
  </div>
  <div class="info-item" v-if="capacity">
    <span class="icon">👥</span>
    <span class="text">限{{ capacity }}人</span>
  </div>
  <div class="info-item" v-if="fee !== undefined">
    <span class="icon">💰</span>
    <span class="text">{{ fee === 0 ? '免费' : `¥${fee}` }}</span>
  </div>
</div>
```

### 第三步：添加样式

```scss
.activity-info {
  margin: 15px 0;
  padding: 10px;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 8px;
  
  .info-item {
    display: flex;
    align-items: center;
    margin: 5px 0;
    font-size: 12px;
    
    .icon {
      margin-right: 5px;
    }
    
    .text {
      flex: 1;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }
  }
}
```

### 第四步：在 ActivityCreate.vue 中使用

```vue
<PosterPreview
  :content="activityForm.description"
  :activityTitle="activityForm.title"
  :activityTime="`${activityForm.startTime} - ${activityForm.endTime}`"
  :location="activityForm.location"
  :capacity="activityForm.capacity"
  :fee="activityForm.fee"
  :agenda="activityForm.agenda"
  :schoolName="schoolInfo.name"
  :logoUrl="schoolInfo.logo"
  :phone="schoolInfo.phone"
  :address="schoolInfo.address"
/>
```

---

## 预期效果

### 修改前
```
┌─────────────────────┐
│   幼儿园 Logo       │
│   幼儿园名称        │
│                     │
│   活动描述...       │
│   (120字符)         │
│                     │
│   📞 电话           │
│   📍 地址           │
│   [二维码]          │
└─────────────────────┘
```

### 修改后
```
┌─────────────────────┐
│   幼儿园 Logo       │
│   幼儿园名称        │
│                     │
│   活动标题          │
│   活动描述...       │
│                     │
│   📅 2024-01-15     │
│   📍 活动地点       │
│   👥 限50人         │
│   💰 ¥99元          │
│                     │
│   📞 电话           │
│   📍 地址           │
│   [二维码]          │
└─────────────────────┘
```

---

## 实施步骤

1. ✅ 修改 `PosterPreview.vue` Props 定义
2. ✅ 更新模板显示活动信息
3. ✅ 添加样式
4. ✅ 在 `ActivityCreate.vue` 中传递数据
5. ✅ 在 `ActivityDetail.vue` 中传递数据
6. ✅ 测试预览效果

---

## 影响范围

- **修改文件**: 2个
  - `PosterPreview.vue`
  - `ActivityCreate.vue`
  - `ActivityDetail.vue`

- **新增代码**: ~50行
- **删除代码**: 0行
- **破坏性变更**: 无（Props 都是可选的）

