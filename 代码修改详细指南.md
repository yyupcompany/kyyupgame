# 代码修改详细指南

## 📝 修改清单

### 文件1: PosterPreview.vue

**位置**: `unified-tenant-system/client/src/components/preview/PosterPreview.vue`

#### 修改1: 扩展 Props 定义（第78-98行）

```typescript
// 修改前
interface Props {
  content: string
  theme?: string
  schoolName?: string
  logoUrl?: string
  phone?: string
  address?: string
  showQR?: boolean
}

// 修改后
interface Props {
  content: string
  theme?: string
  schoolName?: string
  logoUrl?: string
  phone?: string
  address?: string
  showQR?: boolean
  
  // 新增：活动信息
  activityTitle?: string      // 活动标题
  activityTime?: string       // 活动时间
  location?: string           // 活动地点
  capacity?: number           // 参与人数
  fee?: number               // 活动费用
  agenda?: string            // 活动议程
}
```

#### 修改2: 更新 withDefaults（第91-98行）

```typescript
// 修改前
const props = withDefaults(defineProps<Props>(), {
  theme: 'warm',
  schoolName: '阳光幼儿园',
  logoUrl: '/api/placeholder/60/60',
  phone: '400-123-4567',
  address: '北京市朝阳区xxx街道xxx号',
  showQR: true
})

// 修改后
const props = withDefaults(defineProps<Props>(), {
  theme: 'warm',
  schoolName: '阳光幼儿园',
  logoUrl: '/api/placeholder/60/60',
  phone: '400-123-4567',
  address: '北京市朝阳区xxx街道xxx号',
  showQR: true,
  activityTitle: '',
  activityTime: '',
  location: '',
  capacity: 0,
  fee: 0,
  agenda: ''
})
```

#### 修改3: 在模板中添加活动信息（第20-32行之间）

```vue
<!-- 在 main-content 中添加 -->
<div class="activity-info" v-if="activityTitle">
  <div class="info-item" v-if="activityTime">
    <span class="icon">📅</span>
    <span class="text">{{ activityTime }}</span>
  </div>
  <div class="info-item" v-if="location">
    <span class="icon">📍</span>
    <span class="text">{{ location }}</span>
  </div>
  <div class="info-item" v-if="capacity">
    <span class="icon">👥</span>
    <span class="text">限{{ capacity }}人</span>
  </div>
  <div class="info-item" v-if="fee !== undefined && fee !== null">
    <span class="icon">💰</span>
    <span class="text">{{ fee === 0 ? '免费' : `¥${fee}` }}</span>
  </div>
</div>
```

#### 修改4: 添加样式（在 style 块末尾）

```scss
.activity-info {
  margin: 15px 0;
  padding: 12px;
  background: rgba(255, 255, 255, 0.15);
  border-radius: 8px;
  border-left: 3px solid rgba(255, 255, 255, 0.3);
  
  .info-item {
    display: flex;
    align-items: center;
    margin: 6px 0;
    font-size: 13px;
    line-height: 1.4;
    
    .icon {
      margin-right: 8px;
      font-size: 14px;
    }
    
    .text {
      flex: 1;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }
  }
}
```

---

### 文件2: ActivityCreate.vue

**位置**: `unified-tenant-system/client/src/pages/activity/ActivityCreate.vue`

**修改**: 在调用 PosterPreview 时传递数据

```vue
<!-- 修改前 -->
<PosterPreview
  :content="activityForm.description"
  :schoolName="schoolInfo.name"
  :logoUrl="schoolInfo.logo"
  :phone="schoolInfo.phone"
  :address="schoolInfo.address"
/>

<!-- 修改后 -->
<PosterPreview
  :content="activityForm.description"
  :activityTitle="activityForm.title"
  :activityTime="`${formatDateTime(activityForm.startTime)} - ${formatDateTime(activityForm.endTime)}`"
  :location="activityForm.location"
  :capacity="activityForm.capacity"
  :fee="activityForm.fee"
  :agenda="activityForm.agenda"
  :schoolName="schoolInfo.name"
  :logoUrl="schoolInfo.logo"
  :phone="schoolInfo.phone"
  :address="schoolInfo.address"
/>
```

---

### 文件3: ActivityDetail.vue

**位置**: `unified-tenant-system/client/src/pages/activity/ActivityDetail.vue`

**修改**: 在调用 PosterPreview 时传递数据

```vue
<!-- 修改前 -->
<el-image
  :src="activity.posterUrl"
  :preview-src-list="[activity.posterUrl]"
  fit="contain"
/>

<!-- 修改后 -->
<PosterPreview
  :content="activity.description"
  :activityTitle="activity.title"
  :activityTime="`${formatDateTime(activity.startDate)} - ${formatDateTime(activity.endDate)}`"
  :location="activity.location"
  :capacity="activity.participationTarget"
  :fee="activity.fee"
  :schoolName="schoolInfo.name"
  :logoUrl="schoolInfo.logo"
  :phone="schoolInfo.phone"
  :address="schoolInfo.address"
/>
```

---

## 📊 修改统计

| 文件 | 新增行数 | 修改行数 | 删除行数 |
|------|---------|---------|---------|
| PosterPreview.vue | 50 | 10 | 0 |
| ActivityCreate.vue | 0 | 8 | 0 |
| ActivityDetail.vue | 0 | 8 | 0 |
| **总计** | **50** | **26** | **0** |

---

## ✅ 验证清单

- [ ] 修改 PosterPreview.vue Props
- [ ] 添加活动信息模板
- [ ] 添加样式
- [ ] 在 ActivityCreate.vue 传递数据
- [ ] 在 ActivityDetail.vue 传递数据
- [ ] 本地测试预览效果
- [ ] 测试不同活动类型
- [ ] 测试响应式布局
- [ ] 提交代码审查

