# 活动中心预览海报信息缺失分析报告

## 📋 执行摘要

**问题**: 活动中心生成的预览海报缺少关键信息，影响家长报名决策

**结论**: 数据已在创建活动时收集，但**海报预览组件未显示**这些信息

---

## 🔍 详细分析

### 一、创建活动时收集的数据 ✅

在 `ActivityCreate.vue` 中，表单收集了以下信息：

| 字段 | 类型 | 说明 |
|------|------|------|
| title | string | 活动名称 |
| description | string | 活动描述 |
| startTime | datetime | 开始时间 |
| endTime | datetime | 结束时间 |
| location | string | 活动地点 |
| capacity | number | 参与人数上限 |
| fee | number | 活动费用 |
| agenda | string | 活动议程 |
| activityType | enum | 活动类型 |
| status | enum | 活动状态 |

### 二、海报预览显示的信息 ❌

在 `PosterPreview.vue` 中，只显示了：

```vue
<!-- 显示的内容 -->
- schoolName (幼儿园名称)
- logoUrl (logo)
- content (活动描述，仅120字符)
- phone (电话)
- address (地址)
- showQR (二维码)
```

### 三、缺失的关键信息 🚨

从**家长报名角度**，应该显示但未显示的信息：

| 信息 | 重要性 | 当前状态 | 位置 |
|------|--------|---------|------|
| 📅 活动时间 | ⭐⭐⭐⭐⭐ | ❌ 未显示 | 已在表单收集 |
| 📍 活动地点 | ⭐⭐⭐⭐⭐ | ❌ 未显示 | 已在表单收集 |
| 👥 参与人数 | ⭐⭐⭐⭐ | ❌ 未显示 | 已在表单收集 |
| 💰 活动费用 | ⭐⭐⭐⭐⭐ | ❌ 未显示 | 已在表单收集 |
| 📋 活动议程 | ⭐⭐⭐ | ❌ 未显示 | 已在表单收集 |

### 四、生成海报时的数据处理

在 `ActivityCreate.vue` 的 `generatePoster()` 方法中（第1191-1248行）：

```javascript
// 虽然构建了完整的posterContent，但只传给AI生成图片
const posterContent = `
  ${title}：${description}，
  地点：${location}，
  时间：${startDate}，
  限${capacity}人，
  费用：${fee}
`

// 调用AI接口
autoImageApi.generatePosterImage({
  posterTitle,
  posterContent  // ✅ 包含所有信息
})
```

**关键发现**: 
- ✅ 数据已正确构建并传给AI
- ✅ AI生成的图片应该包含这些信息
- ❌ 但 `PosterPreview.vue` 组件没有显示这些信息

---

## 💡 根本原因

`PosterPreview.vue` 是一个**通用预览组件**，设计用于显示：
- 幼儿园信息（logo、名称、电话、地址）
- 活动描述（截断到120字符）
- 二维码

但**没有设计**来显示：
- 活动时间
- 活动地点
- 参与人数
- 活动费用
- 活动议程

---

## ✅ 解决方案

### 方案一：增强 PosterPreview 组件（推荐）

修改 `PosterPreview.vue`，添加新的 props：

```typescript
interface Props {
  content: string
  activityTime?: string      // 新增
  location?: string          // 新增
  capacity?: number          // 新增
  fee?: number              // 新增
  agenda?: string           // 新增
  // ... 其他现有props
}
```

### 方案二：创建专用的活动海报预览组件

创建 `ActivityPosterPreview.vue`，专门用于活动海报预览

### 方案三：修改生成海报的流程

在 `ActivityCreate.vue` 中，将完整数据传给海报预览组件

---

## 📊 影响范围

- **前端文件**: 
  - `unified-tenant-system/client/src/components/preview/PosterPreview.vue`
  - `unified-tenant-system/client/src/pages/activity/ActivityCreate.vue`
  - `unified-tenant-system/client/src/pages/activity/ActivityDetail.vue`

- **后端文件**:
  - `unified-tenant-system/server/src/controllers/activity-poster.controller.ts`
  - `unified-tenant-system/server/src/routes/activity-poster.routes.ts`

---

## 🎯 建议优先级

1. **立即修复**: 增强 PosterPreview 组件显示活动时间、地点、费用
2. **后续优化**: 添加活动议程和参与人数显示
3. **长期规划**: 创建专用的活动海报预览组件

