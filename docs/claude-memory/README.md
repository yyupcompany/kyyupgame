# Claude Code 记忆系统

> 纯JavaScript实现，无需任何依赖的本地记忆系统

## 🎯 特性

- ✅ **零依赖** - 纯JavaScript/Shell脚本，无需npm包
- ✅ **轻量级** - 仅需Node.js 14+，ES模块支持
- ✅ **高性能** - 毫秒级检索，JSON文件存储
- ✅ **智能索引** - 关键词、日期、去重三重索引
- ✅ **一键启动** - 简单脚本即可使用

## 🚀 快速开始

### 前置要求

- Node.js 14+ (支持ES模块)
- Bash shell

### 初始化

```bash
# 进入目录
cd claude-memory

# 初始化数据库
./memory.sh init
```

### 基本使用

```bash
# 生成启动记忆（复制到Claude对话中）
./memory.sh start

# 添加新记录
./memory.sh add

# 搜索记录
./memory.sh search "关键词"

# 查看最近记录
./memory.sh recent
./memory.sh recent 20  # 显示最近20条
```

## 📋 详细命令

### 1. 初始化数据库
```bash
./memory.sh init
```
首次运行时会创建数据文件和索引文件。

### 2. 生成启动记忆
```bash
./memory.sh start           # 默认最近10条
./memory.sh start 5         # 最近5条
./memory.sh start 20        # 最近20条
```
输出的内容复制到Claude对话开头，提供历史上下文。

### 3. 添加记录
```bash
./memory.sh add
```
交互式添加问题和解决方案，会提示输入：
- 问题描述
- 解决方案

### 4. 搜索记录
```bash
./memory.sh search "关键词"
```
基于关键词搜索匹配的记录。

### 5. 查看最近记录
```bash
./memory.sh recent          # 最近10条
./memory.sh recent 20       # 最近20条
```
直接显示最近N条记录。

### 6. 查看帮助
```bash
./memory.sh help
```

## 📁 文件结构

```
claude-memory/
├── memory-db.js           # 核心数据库类
├── memory-cli.js          # 命令行接口
├── memory.sh              # Shell快捷脚本
├── package.json           # NPM配置（可选）
├── README.md              # 说明文档
├── claude-memory.json     # 数据文件（运行时创建）
└── claude-memory-index.json  # 索引文件（运行时创建）
```

## 🔄 数据格式

### 记忆记录结构
```json
{
  "id": "唯一标识符",
  "timestamp": "ISO时间戳",
  "question": "问题描述",
  "solution": "解决方案",
  "tags": ["标签1", "标签2"],
  "hash": "MD5哈希（去重用）",
  "keywords": ["关键词1", "关键词2"]
}
```

### 自动索引
- **关键词索引**: 基于问题+解决方案提取的关键词
- **日期索引**: 按日期分组的记录ID
- **去重索引**: MD5哈希防止重复记录

## 💡 使用场景

### 场景1: 项目开发
记录开发过程中遇到的问题和解决方案，避免重复踩坑。

### 场景2: 学习笔记
保存学习过程中的重要知识点和实践记录。

### 场景3: 问题排查
记录调试过程和解决方案，建立知识库。

## 🔧 高级用法

### 直接使用Node.js
```bash
# 初始化
node memory-cli.js --init

# 搜索
node memory-cli.js --search "关键词"

# 添加（带参数）
node memory-cli.js --add --question "问题" --solution "解决"
```

### 集成到其他脚本
```bash
# 在其他脚本中调用
source ./memory.sh start > memory_prompt.txt
# 然后将 memory_prompt.txt 的内容加入Claude对话
```

### 批量导入
```javascript
// 可以扩展memory-db.js添加批量导入功能
import ClaudeMemoryDB from './memory-db.js'
const db = new ClaudeMemoryDB()
await db.init()

// 批量添加
for (const record of records) {
  await db.add(record.question, record.solution)
}
```

## 📊 性能特点

- **小规模 (<1000条)**: 毫秒级响应 ⚡
- **中等规模 (1000-10000条)**: 10-100毫秒 ✅
- **适合场景**: 个人使用，每天10-50条记录

## 🚀 部署

### 方法1: 复制整个目录
```bash
# 在新机器上
cp -r claude-memory /path/to/new/project/
cd claude-memory
./memory.sh init
```

### 方法2: 仅复制核心文件
```bash
# 只复制必要文件
cp memory-db.js memory-cli.js memory.sh /path/to/new/project/
cd /path/to/new/project
./memory.sh init
```

## 🔍 故障排除

### 常见问题

**Q: 运行报错"Cannot use import statement"**
A: Node.js版本过低，需要14+版本并启用ES模块

**Q: 权限被拒绝**
A: 添加执行权限：`chmod +x memory.sh`

**Q: 数据损坏**
A: 删除索引文件重建：`rm claude-memory-index.json && ./memory.sh init`

### 调试方法
```bash
# 检查数据统计
node memory-cli.js --start

# 查看原始数据
cat claude-memory.json | jq

# 查看索引
cat claude-memory-index.json | jq
```

## 📝 最佳实践

1. **定期备份**
   ```bash
   cp claude-memory.json claude-memory-$(date +%Y%m%d).json
   ```

2. **标签规范**
   - 使用一致的标签格式
   - 例如：#bug-fix, #feature, #learning

3. **问题描述**
   - 清晰描述问题背景
   - 包含关键环境信息

4. **解决方案**
   - 详细记录解决步骤
   - 标注关键注意事项

5. **定期清理**
   ```bash
   # 手动删除过时记录
   # 或编写脚本自动清理
   ```

## 🤝 扩展开发

### 添加新功能

1. 修改 `memory-db.js` 添加新方法
2. 更新 `memory-cli.js` 添加命令支持
3. 在 `memory.sh` 中添加调用

### 示例：添加标签搜索
```javascript
// 在 memory-db.js 中添加
async searchByTag(tag, limit = 20) {
  if (!this.initialized) await this.init()
  const records = this.data.filter(r => r.tags.includes(tag))
  return records
    .sort((a, b) => new Date(b.timestamp) - new Date(a.timestamp))
    .slice(0, limit)
}
```

## 📄 许可证

MIT License - 可自由使用和修改

## 💬 反馈

如有问题或建议，欢迎提交Issue！

---

**Claude 记忆系统** - 让AI对话更智能，让知识沉淀更简单！
